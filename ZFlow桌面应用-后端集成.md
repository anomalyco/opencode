# ZFlow 桌面应用 - 后端集成方案

## 方案对比

### 方案1：独立后端（推荐当前方式）

**架构：**
- 桌面应用：独立运行，连接到外部后端服务
- 后端服务：独立运行在 9999 端口

**优点：**
- 开发灵活，可以分别调试前后端
- 后端可以被多个客户端（Web、桌面、移动）同时使用
- 资源占用小

**使用方式：**
1. 启动后端服务（见开发环境说明）
2. 启动桌面应用
3. 桌面应用自动连接到后端

### 方案2：集成后端（开发模式已实现）

**架构：**
- 桌面应用启动时自动启动后端服务
- 后端作为子进程运行
- 桌面应用关闭时自动关闭后端

**优点：**
- 用户体验更好，一键启动
- 不需要手动管理后端服务

**实现状态：**

- ✅ 开发模式：已实现，使用 wrapper 脚本启动后端
- ⚠️ 生产构建：需要额外配置（见下方说明）

**开发模式使用：**

- 桌面应用会自动启动本地后端服务（通过 sidecar 机制）
- 使用 `packages/opencode` 作为后端源码
- 后端运行在 127.0.0.1:9999

**生产构建挑战：**

- 后端依赖很多外部工具（Playwright、浏览器驱动等）
- 需要将 `packages/opencode` 打包到桌面应用中
- 需要确保 Bun 运行时在用户系统上可用

## 当前推荐使用方案1

桌面应用配置为连接到 `http://192.168.11.14:9999` 的后端。

如需自动启动后端，可以在 Tauri 的 Rust 代码中添加进程启动逻辑（需要修改 src-tauri 中的 Rust 代码）。

## 开发建议

对于开发，建议：
1. 保持后端独立运行
2. 桌面应用连接到后端
3. 使用 `run-desktop-dev.bat` 时确保后端已启动

## 部署建议

如果需要分发给最终用户：
1. 提供安装包（ZFlow_x64-setup.exe）
2. 提供后端服务启动脚本
3. 桌面应用自动连接到后端

或者提供两个安装包：
- ZFlow-Desktop.exe（仅桌面应用）
- ZFlow-Server.exe（后端服务）
