---
description: Execute AI SDLC OS Quality Gate checks (Linting, Typechecks, Unit Tests)
---

# SDLC Quality Gate Workflow

// turbo-all
1. Verify TypeScript types in `packages/core`
2. Verify TypeScript types in `packages/opencode`
3. Run unit tests in `packages/core`
