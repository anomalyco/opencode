#!/usr/bin/env bash

# Run a release locally. Pass a bump type: patch | minor | major (default: patch).
BUMP_TYPE=${1:-patch}

export OPENCODE_BUMP="$BUMP_TYPE"
export OPENCODE_RELEASE="1"

bun script/publish.ts