# Spec: Consolidate context management logic in `packages/tui/src/components/ContextSidebar.tsx` to enable cross-provider memory state sharing.

## Changes
- ADDED: Initial spec stub — to be refined during ANALYSIS
