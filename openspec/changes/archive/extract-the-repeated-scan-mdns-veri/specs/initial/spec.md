# Spec: Extract the repeated "scan mDNS + verify + dedup by name" pattern in packages/core/src/local-providers/ into a reusable discovery service in packages/core/src/discovery/index.ts.

## Changes
- ADDED: Initial spec stub — to be refined during ANALYSIS
