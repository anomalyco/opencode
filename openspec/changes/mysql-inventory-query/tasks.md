## 1. Runtime planning and formatter contract

- [x] 1.1 Verify the approved runtime cleanup in `openspec/config.yaml`, `docs/workflows/openspec-superpowers.md`, `openspec/changes/feishu-chat-gateway/proposal.md`, `openspec/changes/feishu-chat-gateway/design.md`, `openspec/changes/feishu-chat-gateway/specs/conversation-event-log/spec.md`, `openspec/changes/feishu-chat-gateway/tasks.md`, and `docs/superpowers/specs/2026-07-23-feishu-chat-gateway-design.md`. Run `rg -n "T1 只读|T1 SQL Server|T1/MySQL|T1 or MySQL" openspec/config.yaml docs/workflows/openspec-superpowers.md openspec/changes/feishu-chat-gateway docs/superpowers/specs/2026-07-23-feishu-chat-gateway-design.md`; expected: no T1 runtime, future-query, or fallback statement remains, while the two root T1 history files still exist.
- [x] 1.2 RED — Create `packages/feishu/test/inventory-answer.test.ts` with the exact complete example, supplierless inventory, missing optionals, multiple shelves, dimension normalization, multiline remark, multiple result lines, prohibited code/table text, and no-result cases. Run from `packages/feishu` with `bun test test/inventory-answer.test.ts`; expected: FAIL because `src/inventory-answer.ts` does not exist.
- [x] 1.3 GREEN — Create `packages/feishu/src/inventory-answer.ts` exporting the identifier-free `InventoryAnswerItem` type and `formatInventoryAnswer(items)` function. Run from `packages/feishu` with `bun test test/inventory-answer.test.ts`; expected: the exact sentence `6001ZZ（清油）（12×28×8）（货架号：B-11-13）上海涂众轴承库存200，备注：xxx` passes, multiple items use one newline, and output contains no code/table/preamble.

## 2. Inventory mapping

- [x] 2.1 RED — Create `packages/feishu/test/inventory-mapper.test.ts` with real MySQL-shaped fixtures covering `Product.s_ID` identity, duplicate names, `SUM(Storage.Prod_Number1)`, dimension extraction, shelf-only `ProdType` exclusion, ordered unique shelves, supplier source projection, `UNATTRIBUTED`, disabled/deleted suppliers, malformed decimals, and remark isolation. Run from `packages/feishu` with `bun test test/inventory-mapper.test.ts`; expected: FAIL because `src/inventory-mapper.ts` does not exist.
- [x] 2.2 GREEN — Create `packages/feishu/src/inventory-mapper.ts` exporting row input types and `mapInventoryRows(...)`, returning only `InventoryAnswerItem[]` with no internal identifiers. Run from `packages/feishu` with `bun test test/inventory-mapper.test.ts test/inventory-answer.test.ts`; expected: supplier-attributed rows split deterministically, unattributed stock falls back to one product-total item, and all mapping/format tests pass.
- [x] 2.3 Add a regression case for the read-only observed `6001ZZ` row (`u_Name=6001ZZ`, `ProdType=清油`, `ProdSpec=12*28*8`, Storage total `200`) to `packages/feishu/test/inventory-mapper.test.ts`. Run from `packages/feishu` with `bun test test/inventory-mapper.test.ts -t 6001ZZ`; expected: the mapped and formatted result contains `6001ZZ（清油）（12×28×8）库存200` before any optional shelf, supplier, or remark fragments.

## 3. Package configuration and MySQL preflight

- [x] 3.1 RED — Create `packages/feishu/test/mysql-config.test.ts` covering required MySQL host, port, database, user, controlled password-file path, sanitized missing-field errors, and absence of SQL Server/T1 configuration. Run from `packages/feishu` with `bun test test/mysql-config.test.ts`; expected: FAIL because `src/mysql-config.ts` does not exist.
- [x] 3.2 GREEN — Create or extend `packages/feishu/package.json` and `packages/feishu/tsconfig.json`, add exact `mysql2@3.14.4`, create `packages/feishu/src/mysql-config.ts`, and update `bun.lock` with `bun install` from `D:\opencode`. Then run from `packages/feishu` with `bun test test/mysql-config.test.ts && bun typecheck`; expected: configuration tests pass, no secret value or complete connection string is printed, and the package type-checks.
- [x] 3.3 RED — Create `packages/feishu/test/mysql-preflight.test.ts` for MySQL version/database/account/read-only observation, required Product/Storage/shelf/source columns, schema-version capture, incompatible type, missing view, wrong database, and secret-bearing driver error sanitization. Run from `packages/feishu` with `bun test test/mysql-preflight.test.ts`; expected: FAIL because `src/mysql-preflight.ts` does not exist.
- [x] 3.4 GREEN — Create `packages/feishu/src/mysql-preflight.ts` using an injected narrow query executor and exact schema expectations from `specs/mysql-inventory-read/spec.md`. Run from `packages/feishu` with `bun test test/mysql-preflight.test.ts`; expected: compatible MySQL enables the adapter and every identity/schema mismatch fails closed without fallback.

## 4. Prepared MySQL inventory query

- [x] 4.1 RED — Create `packages/feishu/test/mysql-inventory.test.ts` covering validated product terms, prepared placeholders, bounded deterministic ordering, Product/Storage/shelf joins, optional source/partner resolution, no-result output, query timeout, malformed row failure, and rejection of writes/arbitrary SQL. Run from `packages/feishu` with `bun test test/mysql-inventory.test.ts`; expected: FAIL because `src/mysql-inventory.ts` does not exist.
- [x] 4.2 GREEN — Create `packages/feishu/src/mysql-inventory.ts` with a `mysql2/promise` pool, versioned fixed `SELECT` templates, decimal strings, bounded query timeout, result mapping, and explicit disposal. Run from `packages/feishu` with `bun test test/mysql-inventory.test.ts test/inventory-mapper.test.ts test/inventory-answer.test.ts`; expected: only approved reads are reachable and mapped output is deterministic and identifier-free.
- [x] 4.3 Create `packages/feishu/test/mysql-inventory.contract.test.ts` and a `test:mysql-contract` package script gated by explicit local contract configuration. Run from `packages/feishu` against the approved MySQL password file with `bun run test:mysql-contract`; expected: MySQL `8.4.x`, configured schema identity, required fields, `6001ZZ` total `200`, structured shelf reads, and zero database writes are verified without printing credentials.

## 5. Trusted service boundary and gateway integration

- [x] 5.1 RED — Create `packages/feishu/test/inventory-tool.test.ts` covering valid trusted context, missing/expired/forged context, empty product term clarification, fixed tool input, model-authored SQL rejection, database write rejection, and single-sentence sanitized failures. Run from `packages/feishu` with `bun test test/inventory-tool.test.ts`; expected: FAIL because `src/inventory-tool.ts` does not exist.
- [x] 5.2 GREEN — Create `packages/feishu/src/inventory-tool.ts` that validates trusted Feishu context before pool acquisition, invokes only the inventory adapter, and returns formatter output verbatim. Run from `packages/feishu` with `bun test test/inventory-tool.test.ts test/mysql-inventory.test.ts`; expected: invalid context reaches no query port and approved requests cannot alter SQL or output format.
- [x] 5.3 After the `feishu-chat-gateway` admission and worker files exist, RED — extend `packages/feishu/test/agent.test.ts`, `packages/feishu/test/worker.test.ts`, and `packages/feishu/test/gateway.test.ts` so the Agent remains zero-tool/default-deny, recognized inventory/location requests are routed before OpenCode, and the formatter string is sent without any provider turn. Run from `packages/feishu` with `bun test test/agent.test.ts test/worker.test.ts test/gateway.test.ts`; expected: FAIL until runtime routing is added.
- [x] 5.4 After task 5.3, GREEN — modify `packages/feishu/src/worker.ts`, `packages/feishu/src/gateway.ts`, and `packages/feishu/src/index.ts` to compose the trusted pre-model inventory route and send its fixed text result verbatim while leaving `.opencode/agent/feishu-chat.md` at zero tools. Run from `packages/feishu` with `bun test test/agent.test.ts test/worker.test.ts test/gateway.test.ts`; expected: inventory/location requests query MySQL once without invoking OpenCode, ordinary chat still uses DeepSeek, forbidden capabilities remain blocked, and the final inventory reply has one product per line.

## 6. Trace events and gold-case gate

- [x] 6.1 RED — Create `packages/feishu/test/inventory-trace.test.ts` and `packages/feishu/test/fixtures/inventory-gold.json` covering full/sentence messages, trusted-context admission, intent, versioned SQL template, sanitized parameters, schema version, row count, mapped result, final exact answer, failure, feedback, linked correction, secret canaries, schema drift, and an attempted write. Run from `packages/feishu` with `bun test test/inventory-trace.test.ts`; expected: FAIL until inventory trace events exist.
- [x] 6.2 GREEN — Create `packages/feishu/src/inventory-trace.ts` and, after the gateway logger exists, extend `packages/feishu/src/event-log.ts` to append inventory events using the existing conversation/turn/trace/message/sentence/parent/related identifiers. Run from `packages/feishu` with `bun test test/inventory-trace.test.ts test/event-log.test.ts`; expected: the four layers are reconstructable, corrections append rather than overwrite, and no credential, password path, full connection string, hidden reasoning, or raw driver secret reaches logs.
- [x] 6.3 Create `packages/feishu/src/inventory-eval.ts` and `packages/feishu/test/inventory-eval.test.ts` to score intent, SQL, result, and answer layers separately and block release below 95% read accuracy; policy/write/secret cases require 100%. Run from `packages/feishu` with `bun test test/inventory-eval.test.ts`; expected: a 94% fixture fails the gate and the approved gold fixture passes every layer.

## 7. Documentation and verification

- [x] 7.1 Update `packages/feishu/.env.example` and `packages/feishu/README.md` with MySQL-only configuration names, password-file handling, schema preflight, fixed answer example, no-result/failure wording, contract-test command, and explicit absence of SQL Server fallback. Run from `packages/feishu` with `bun test test/readme.test.ts`; expected: documentation assertions pass and no credential value is present.
- [x] 7.2 Run package verification from `packages/feishu` with `bun test && bun typecheck && bunx oxlint src test`; expected: all automated tests pass, type-checking and lint succeed, normal tests contact no real Feishu, DeepSeek, or database, and only the explicit contract command contacts MySQL.
- [x] 7.3 Run strict OpenSpec and repository checks from `D:\opencode` with `openspec-cn validate mysql-inventory-query --type change --strict --json; openspec-cn validate feishu-chat-gateway --type change --strict --json; git diff --check; git status --short`; expected: both changes are valid, whitespace checks pass, the two T1 history files remain present, and only intentional planning/package changes are listed.
- [x] 7.4 Use `superpowers:verification-before-completion` and `openspec-verify-change` to compare implementation evidence with both `mysql-inventory-read` and `inventory-answer-format` specs. Expected: no critical gaps remain, the exact approved sentence passes, no internal code/table/T1 path exists, four-layer read accuracy is at least 95%, policy and secret cases are 100%, and any unmet dependency on `feishu-chat-gateway` is reported as incomplete rather than waived.

## 8. Migrated purchase supplier fallback

- [x] 8.1 RED — Extend `packages/feishu/test/inventory-mapper.test.ts` with a supplier fallback row and cases proving that an active structured source wins, an otherwise valid fallback uses the product total, and no reliable supplier still omits the name. Run from `packages/feishu` with `bun test test/inventory-mapper.test.ts`; expected: FAIL because the mapper does not accept purchase supplier fallbacks.
- [x] 8.2 GREEN — Extend `packages/feishu/src/inventory-mapper.ts` with an identifier-free purchase supplier fallback input and apply it only when no active structured source exists. Run from `packages/feishu` with `bun test test/inventory-mapper.test.ts test/inventory-answer.test.ts`; expected: the actual fallback supplier precedes `库存`, structured source quantities remain authoritative, and internal IDs remain absent.
- [x] 8.3 RED — Extend `packages/feishu/test/mysql-inventory.test.ts` with a fourth fixed query response and assertions for the migrated `ListBuy`/`MasterBill`/`Units` purchase lookup, valid-bill filters, deterministic latest-row ordering, strict row validation, and query-template version `mysql-inventory-v2`. Run from `packages/feishu` with `bun test test/mysql-inventory.test.ts`; expected: FAIL because no purchase supplier query exists.
- [x] 8.4 GREEN — Extend `packages/feishu/src/mysql-inventory.ts` with one parameterized purchase supplier query over only the selected product IDs. Accept only `MasterBill.BillState=3`, `MasterBill.s_Syb=0`, positive `ListBuy.Prod_Number`, and non-empty `Units.u_Name`; select one row per product by `MasterBill.BillDate DESC`, `MasterBill.AutoID DESC`, and `ListBuy.List_ID DESC`; pass the result to the mapper and bump query events to `mysql-inventory-v2`. Run from `packages/feishu` with `bun test test/mysql-inventory.test.ts test/inventory-mapper.test.ts`; expected: the reader returns actual fallback suppliers without exposing purchase or product identifiers.
- [x] 8.5 RED — Extend `packages/feishu/test/mysql-preflight.test.ts` to require the exact `ListBuy`, `MasterBill`, and `Units` fallback columns and contract version `mysql-inventory-v2`. Run from `packages/feishu` with `bun test test/mysql-preflight.test.ts`; expected: FAIL until the preflight contract includes the new read dependency.
- [x] 8.6 GREEN — Update `packages/feishu/src/mysql-preflight.ts` and affected trace/gold fixtures for `mysql-inventory-v2`. Run from `packages/feishu` with `bun test test/mysql-preflight.test.ts test/inventory-trace.test.ts test/inventory-eval.test.ts`; expected: compatible MySQL enables the adapter, drift fails closed, and the new query version is traceable without secrets.
- [x] 8.7 Run the explicit live MySQL contract query and confirm the current-total `177` `6001ZZ` maps to `虎旺轴承` while the `清油` current-total `200` row maps to `天宇轴承`; confirm the literal formatting-only supplier example is not fabricated. Do not print credentials, password paths, or internal product IDs in the acceptance record.
- [x] 8.8 Run `bun test`, `bun typecheck`, and the package's pinned `bun run lint` command from `packages/feishu`, then run strict OpenSpec validation and `git diff --check` from the repository root. After all checks pass, safely restart the Feishu gateway and verify one fresh WebSocket-ready event with no post-ready stderr errors.
