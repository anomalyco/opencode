## 1. Package and configuration boundary

- [x] 1.1 RED — Create `packages/feishu/test/config.test.ts` and `packages/feishu/test/agent.test.ts` for required field names, sanitized configuration failures, DeepSeek-only model validation, and the `feishu-chat` Agent tool/permission policy. Verify from `packages/feishu` with `bun test test/config.test.ts test/agent.test.ts`; expected: tests fail because the package configuration and Agent do not exist.
- [x] 1.2 GREEN — Create `packages/feishu/package.json`, `packages/feishu/tsconfig.json`, `packages/feishu/.env.example`, `packages/feishu/src/config.ts`, and `.opencode/agent/feishu-chat.md`; add pinned `@larksuiteoapi/node-sdk@1.71.1`, workspace `@opencode-ai/sdk-next`, and required Effect/Bun development dependencies, then update `bun.lock`. Verify from `packages/feishu` with `bun test test/config.test.ts test/agent.test.ts && bun typecheck`; expected: configuration and Agent tests pass, missing values are never printed, non-DeepSeek models are rejected, and the package type-checks.
- [x] 1.3 Document the exact local start command, Feishu application capabilities/scopes, DeepSeek preflight, data location, and secret-handling rules in `packages/feishu/README.md`. Verify from `packages/feishu` with `bun test test/readme.test.ts` after adding `packages/feishu/test/readme.test.ts`; expected: the README names `im.message.receive_v1`, single-chat and group-mention permissions, one start command, no public callback requirement, and no credential values.

## 2. Deterministic identity and sentence handling

- [x] 2.1 RED — Create `packages/feishu/test/identity.test.ts` covering direct-chat keys, group `thread_id`/`root_id`/`message_id` fallback, cross-conversation isolation, versioned hashed persistence IDs, stable Session IDs, and stable prompt message IDs. Verify from `packages/feishu` with `bun test test/identity.test.ts`; expected: tests fail before `packages/feishu/src/identity.ts` exists.
- [x] 2.2 GREEN — Implement normalized routing and deterministic cryptographic identifiers in `packages/feishu/src/identity.ts`, using valid OpenCode Session/message ID prefixes without persisting raw conversation keys. Verify from `packages/feishu` with `bun test test/identity.test.ts`; expected: repeated inputs are stable across fresh instances and all isolation/fallback cases pass.
- [x] 2.3 RED/GREEN — Create `packages/feishu/test/sentence.test.ts` and implement `packages/feishu/src/sentence.ts` for deterministic punctuation/newline segmentation, ordered stable sentence IDs, and whole-message fallback without rewriting. Verify from `packages/feishu` with `bun test test/sentence.test.ts`; expected: Chinese/English punctuation and newlines preserve exact ordered content, while unsegmentable text produces one sentence at index zero.

## 3. SQLite task store and append-only event log

- [x] 3.1 RED — Create `packages/feishu/test/store.test.ts` covering schema migration, atomic receipt admission, unique Feishu message identity, state-transition events, non-terminal recovery queries, delivered-task exclusion, conflict detection, and event immutability. Verify from `packages/feishu` with `bun test test/store.test.ts`; expected: tests fail before the store exists.
- [x] 3.2 GREEN — Implement the Bun SQLite schema, additive migrations, `gateway_task` current-state operations, and insert-only `gateway_event` operations in `packages/feishu/src/store.ts` and `packages/feishu/src/migrations.ts`; keep the database under the configured OpenCode local data directory with WAL and foreign keys. Verify from `packages/feishu` with `bun test test/store.test.ts`; expected: atomic transition/recovery tests pass and no API can update or delete a committed event.
- [x] 3.3 RED — Create `packages/feishu/test/event-log.test.ts` covering complete user/assistant messages, ordered sentence events, one reconstructable trace, Agent/model/prompt/execution/delivery evidence, linked corrections, blocked tool evidence, forward-compatible tool/SQL event envelopes, and absence of runtime SQL execution in this change. Verify from `packages/feishu` with `bun test test/event-log.test.ts`; expected: tests fail before the event logger exists.
- [x] 3.4 GREEN — Implement trace construction and append-only event recording in `packages/feishu/src/event-log.ts`, including `conversation_id`, `turn_id`, `trace_id`, message/sentence/parent/related IDs, versions, status, duration, and sanitized structured content. Verify from `packages/feishu` with `bun test test/event-log.test.ts`; expected: successful, failed, corrected, policy-blocked, and future tool/SQL evidence can be reconstructed without modifying prior events.
- [x] 3.5 RED/GREEN — Create `packages/feishu/test/sanitize.test.ts` and implement `packages/feishu/src/sanitize.ts` plus the independent fallback JSONL sink in `packages/feishu/src/fallback-log.ts`; test configured secret canaries, common credential fields, credential-bearing upstream errors, hidden reasoning exclusion, ordinary business text preservation, and primary-log failure behavior. Verify from `packages/feishu` with `bun test test/sanitize.test.ts`; expected: no canary reaches SQLite, fallback output, console payloads, error replies, or snapshots, while non-secret business text remains unchanged.

## 4. Feishu Channel adapter

- [ ] 4.1 RED — Create `packages/feishu/test/feishu-channel.test.ts` with official-SDK-shaped fixtures for direct text, mentioned group text, unmentioned group text, mention removal, unsupported content, thread reply targets, connection lifecycle, and sanitized handshake failures. Verify from `packages/feishu` with `bun test test/feishu-channel.test.ts`; expected: tests fail before the adapter exists.
- [ ] 4.2 GREEN — Implement the pinned official Channel integration and narrow normalized message/reply ports in `packages/feishu/src/feishu-channel.ts`; configure open direct chats, required group mentions, WebSocket mode, SDK reconnection observation, and final text replies. Verify from `packages/feishu` with `bun test test/feishu-channel.test.ts`; expected: only supported messages cross the normalized port, reply targets are correct, and raw SDK objects or secrets do not enter domain events.
- [ ] 4.3 RED/GREEN — Add `packages/feishu/test/admission.test.ts` and implement the callback admission path in `packages/feishu/src/admission.ts` so accepted messages atomically persist complete/sentence events and a task before returning, while model work remains outside the callback. Verify from `packages/feishu` with `bun test test/admission.test.ts`; expected: persistence happens before callback completion, persistence failure schedules no model work, and a delayed model port does not delay callback completion.

## 5. Embedded OpenCode and restricted model execution

- [ ] 5.1 RED — Create `packages/feishu/test/opencode.test.ts` using the real scoped `packages/sdk-next` embedded router with a temporary Location/test provider to cover deterministic Session creation/adoption, fixed Agent/model reconciliation, deterministic prompt admission, durable event replay, projected final text, Token/cost metadata, and exact prompt retry. Verify from `packages/feishu` with `bun test test/opencode.test.ts`; expected: tests fail before `packages/feishu/src/opencode.ts` exists.
- [ ] 5.2 GREEN — Implement the process-scoped OpenCode adapter in `packages/feishu/src/opencode.ts` using `sessions.create/get/prompt/events/wait/message/interrupt`, with no listener or child process and with one explicit provider turn per prompt. Verify from `packages/feishu` with `bun test test/opencode.test.ts`; expected: Session continuity and exact retry tests pass and the final answer is derived from the admitted turn's durable projection.
- [ ] 5.3 RED/GREEN — Extend `packages/feishu/test/opencode.test.ts` with forbidden user requests and a synthetic tool-call event, then implement policy handling in `packages/feishu/src/opencode.ts`. Verify from `packages/feishu` with `bun test test/opencode.test.ts -t tool`; expected: the Agent exposes zero tools, every permission defaults to deny, a tool event interrupts the Session and appends `operation_blocked`, and no execution port or tool result is reached.
- [ ] 5.4 RED/GREEN — Add model failure, timeout, rate-limit, authentication failure, empty-final-text, and reasoning-exclusion cases to `packages/feishu/test/opencode.test.ts`, then implement sanitized failure classification in `packages/feishu/src/opencode.ts`. Verify from `packages/feishu` with `bun test test/opencode.test.ts -t failure`; expected: each failure is classified with retryability and trace context, produces no invented answer, and stores no reasoning or credential content.

## 6. Worker ordering, recovery, and delivery semantics

- [ ] 6.1 RED — Create `packages/feishu/test/worker.test.ts` covering same-Session FIFO, different-Session concurrency, duplicate delivery coalescing, conflicting reuse, restart from `received`, reconciliation after prompt admission, delivered-task suppression, and isolation after one task fails. Verify from `packages/feishu` with `bun test test/worker.test.ts`; expected: tests fail before the worker exists.
- [ ] 6.2 GREEN — Implement the keyed queue, bounded cross-Session concurrency, state-machine orchestration, startup recovery scan, and exact-retry reconciliation in `packages/feishu/src/worker.ts`. Verify from `packages/feishu` with `bun test test/worker.test.ts`; expected: per-Session order is deterministic, different Sessions overlap, duplicates invoke the model once, and restart cases converge without duplicate user messages.
- [ ] 6.3 RED/GREEN — Extend `packages/feishu/test/worker.test.ts` for confirmed delivery, explicit retryable non-delivery, bounded retry exhaustion, and network-uncertain delivery; implement delivery transitions in `packages/feishu/src/worker.ts`. Verify from `packages/feishu` with `bun test test/worker.test.ts -t delivery`; expected: delivered tasks never resend, only confirmed non-delivery retries, and uncertain outcomes stop in `uncertain_delivery` for manual review.

## 7. Process wiring and end-to-end package verification

- [ ] 7.1 RED — Create `packages/feishu/test/gateway.test.ts` that composes a fake Channel, real temporary SQLite, embedded/test OpenCode adapter, and fake reply client for direct chat, group mention, unmentioned group silence, context continuity, cross-chat isolation, duplicate delivery, restart recovery, tool blocking, complete trace reconstruction, and secret canaries. Verify from `packages/feishu` with `bun test test/gateway.test.ts`; expected: tests fail before the application layer and entrypoint exist.
- [ ] 7.2 GREEN — Implement `packages/feishu/src/gateway.ts` and `packages/feishu/src/index.ts` to compose configuration, SQLite, event logging, Channel, worker, scoped OpenCode host, graceful shutdown, and sanitized process-level failure reporting. Verify from `packages/feishu` with `bun test test/gateway.test.ts`; expected: all end-to-end cases pass without network access, database access, tool execution, duplicate replies, or secret leakage.
- [ ] 7.3 Run package verification from `packages/feishu` with `bun test && bun typecheck && bunx oxlint src test`; expected: all tests pass, type-checking succeeds, lint reports no errors, and no test contacts real Feishu, DeepSeek, or MySQL.
- [ ] 7.4 Run repository/change verification from `D:\opencode` with `openspec-cn validate feishu-chat-gateway --type change --strict --json; git diff --check; git status --short`; expected: strict validation reports the single change valid, no whitespace errors exist, and only intentional project/config/design/package changes are listed.

## 8. Real local acceptance and handoff

- [ ] 8.1 Run the documented startup preflight against the existing local OpenCode authentication without sending a message; expected: the selected provider/model resolves to DeepSeek, required Feishu field names are present, secrets are not printed, and no public listener or database connection is opened.
- [ ] 8.2 With the published Feishu test application, execute the eleven acceptance cases from `design.md`: direct reply, mentioned group-thread reply, unmentioned group silence, same-chat memory, cross-chat isolation, duplicate event suppression, restart continuity, forbidden-tool blocking, complete trace, secret absence, and one-command startup. Expected: every case has a recorded trace and passes; any failure blocks completion.
- [ ] 8.3 Inspect the gateway SQLite through a read-only verification command documented in `packages/feishu/README.md`; expected: complete and sentence-level messages share the trace, state transitions are append-only, tool requests are blocked with no tool/SQL execution, correction links can be appended, and no authentication secret or hidden reasoning is present.
- [ ] 8.4 Use `superpowers:verification-before-completion` and `openspec-verify-change` to compare the implementation and evidence against all four capability specs. Expected: no critical gaps remain, all tasks and automated checks are complete, database accuracy thresholds are recorded as not applicable to this database-free change, and the change is ready for user-approved archive or publication.
