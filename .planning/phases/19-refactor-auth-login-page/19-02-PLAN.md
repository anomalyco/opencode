---
phase: 19-refactor-auth-login-page
plan: 02
type: execute
wave: 2
depends_on: ["19-refactor-auth-login-page-01"]
files_modified:
  - packages/opencode/src/server/ui-dir.ts
  - packages/opencode/src/server/server.ts
  - packages/opencode/src/server/routes/auth.ts
autonomous: true
must_haves:
  truths:
    - "GET /auth/login serves the SolidJS-based login page"
    - "Security context (warning/block) still controls the login UX"
    - "String-based login page template is removed from auth routes"
  artifacts:
    - path: "packages/opencode/src/server/ui-dir.ts"
      provides: "Shared UI directory getter/setter for route access"
      exports: ["setUiDir", "getUiDir"]
    - path: "packages/opencode/src/server/routes/auth.ts"
      provides: "Login route rendering Solid login HTML"
      contains: "login.html"
    - path: "packages/opencode/src/server/server.ts"
      provides: "UI directory stored for route access"
      contains: "setUiDir"
  key_links:
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/opencode/src/server/ui-dir.ts"
      via: "getUiDir()"
      pattern: "getUiDir"
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "packages/app/dist/login.html"
      via: "filesystem read and HTML injection"
      pattern: "login.html"
    - from: "packages/opencode/src/server/routes/auth.ts"
      to: "getConnectionSecurityInfo"
      via: "bootstrap data injection"
      pattern: "shouldWarn|shouldBlock"
---

<objective>
Serve the built SolidJS login page from the auth route and remove the existing string template.

Purpose: Ensure `/auth/login` uses the new Solid login entry while preserving the existing security context and behaviors.
Output: Auth route reads `login.html` from the built UI and injects login bootstrap data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/19-refactor-auth-login-page/19-CONTEXT.md
@.planning/phases/19-refactor-auth-login-page/19-RESEARCH.md
@packages/opencode/src/server/server.ts
@packages/opencode/src/server/routes/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared UI directory access for auth routes</name>
  <files>packages/opencode/src/server/ui-dir.ts, packages/opencode/src/server/server.ts</files>
  <action>
Create `ui-dir.ts` with `setUiDir(path?: string)` and `getUiDir(): string | undefined` to store the resolved UI directory.
Update `Server.listen` in `server.ts` to call `setUiDir` when `uiDir` is provided (keep the existing `_uiDir` usage for static serving).
This avoids circular imports while allowing auth routes to access the UI build output path.
  </action>
  <verify>In `packages/opencode`, run `bun run typecheck` to ensure new module imports are valid.</verify>
  <done>Auth routes can read the UI directory path without importing `server.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Serve Solid login HTML and remove the template string</name>
  <files>packages/opencode/src/server/routes/auth.ts</files>
  <action>
Replace `generateLoginPageHtml` with a loader that reads the built `login.html`:
- Use `getUiDir()` to resolve the UI build directory.
- Read `login.html` from the UI dir (cache the template string for reuse).
- Inject a bootstrap script such as `window.__OPENCODE_LOGIN__ = { shouldWarn, shouldBlock, isSecure }` before the closing `</head>` or `</body>`.
- Return the resulting HTML via `c.html()` for the `/auth/login` route.
- If the UI dir or `login.html` is missing, return a clear error response (do not fall back to the old template).
Keep existing `getConnectionSecurityInfo` logic and make sure the login page still receives the same warning/block context.
  </action>
  <verify>Run `bun run build` in `packages/app` and confirm `/auth/login` serves `login.html` in local mode.</verify>
  <done>Auth route serves the Solid login page and no large login HTML template remains in `auth.ts`.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` in `packages/opencode` succeeds.
- `bun run build` in `packages/app` produces `dist/login.html` that is served for `/auth/login`.
</verification>

<success_criteria>
- `/auth/login` responds with the SolidJS login page using injected security context.
- The string-based login HTML template is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/19-refactor-auth-login-page/19-02-SUMMARY.md`
</output>
