---
phase: 21-ssh-key-management
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - packages/sdk/openapi.json
  - packages/sdk/js/src/v2/gen/client.gen.ts
  - packages/sdk/js/src/v2/gen/sdk.gen.ts
  - packages/sdk/js/src/v2/gen/types.gen.ts
  - packages/sdk/js/src/v2/gen/core/serverSentEvents.gen.ts
  - packages/app/src/components/settings/ssh-keys-dialog.tsx
  - packages/app/src/components/settings/settings-dialog.tsx
  - packages/app/src/components/repo/clone-dialog.tsx
  - packages/app/src/pages/layout.tsx
autonomous: true
must_haves:
  truths:
    - "Users can add, view, and delete SSH keys from the settings UI"
    - "Clone dialog prompts to add an SSH key when using SSH and no keys exist"
    - "SDK exposes SSH key endpoints for the webapp"
  artifacts:
    - path: "packages/app/src/components/settings/ssh-keys-dialog.tsx"
      provides: "SSH key CRUD UI and form"
      contains: "SSH keys"
    - path: "packages/app/src/components/repo/clone-dialog.tsx"
      provides: "SSH key missing prompt in clone flow"
      contains: "Add SSH key"
    - path: "packages/sdk/openapi.json"
      provides: "Updated API spec for SSH keys"
      contains: "/ssh-keys"
  key_links:
    - from: "packages/app/src/components/settings/ssh-keys-dialog.tsx"
      to: "/ssh-keys"
      via: "globalSDK client"
      pattern: "client\\.ssh"
    - from: "packages/app/src/components/repo/clone-dialog.tsx"
      to: "packages/app/src/components/settings/ssh-keys-dialog.tsx"
      via: "settings dialog launch"
      pattern: "Settings"
---

<objective>
Expose the SSH key APIs in the SDK and build the settings UI plus clone prompt for missing SSH keys.

Purpose: Let users manage SSH keys from the webapp and guide them when SSH cloning needs a key.
Output: Updated SDK client, settings dialog UI, and clone flow prompt integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@packages/app/src/components/repo/clone-dialog.tsx
@packages/app/src/hooks/use-clone-progress.ts
@packages/app/src/pages/layout.tsx
@packages/app/src/components/repo/repository-manager-dialog.tsx
@packages/sdk/js/script/build.ts
@script/generate.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate OpenAPI and SDK clients</name>
  <files>packages/sdk/openapi.json, packages/sdk/js/src/v2/gen/*</files>
  <action>
Regenerate the OpenAPI spec and SDK clients after adding the SSH key routes:
- Run `./script/generate.ts` from the repo root to refresh `packages/sdk/openapi.json` and the JS SDK outputs.
- Ensure the generated client includes SSH key endpoints (list/create/delete) and types for the new schemas.
Avoid manual edits to generated files.
  </action>
  <verify>Run `./script/generate.ts` and confirm SDK outputs include `/ssh-keys` operations.</verify>
  <done>OpenAPI + SDK files are updated and include SSH key endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Build settings UI for SSH key CRUD</name>
  <files>packages/app/src/components/settings/ssh-keys-dialog.tsx, packages/app/src/components/settings/settings-dialog.tsx, packages/app/src/pages/layout.tsx</files>
  <action>
Add a Settings dialog with an SSH key management section:
- Create `SettingsDialog` that renders a "SSH keys" section (list + add form) and uses `useDialog` so it can be launched from the sidebar.
- Implement `SshKeysDialog` with a `createStore` form state for name, hosts (comma-separated), privateKey, publicKey.
- Fetch existing keys on load via `globalSDK.client` and render name, fingerprint, hosts, and install status.
- Add actions to create and delete keys with `showToast` on success/failure; refetch list after mutations.
- Wire the sidebar settings icon in `packages/app/src/pages/layout.tsx` to open `SettingsDialog` and remove the disabled state.
Keep styling consistent with existing dialog patterns (e.g., repo manager).
  </action>
  <verify>Run `bun run build` in `packages/app` and ensure it compiles.</verify>
  <done>Users can manage SSH keys from a settings dialog launched via the sidebar.</done>
</task>

<task type="auto">
  <name>Task 3: Prompt for SSH key when cloning without one</name>
  <files>packages/app/src/components/repo/clone-dialog.tsx</files>
  <action>
Add missing-SSH-key prompts to the clone flow:
- Detect SSH URLs (`git@` or `ssh://`) and check `globalSDK.client.sshKeys.list()` for existing keys.
- When the user attempts an SSH clone with zero keys, show a warning block with an "Add SSH key" button that opens the settings dialog.
- On SSH clone errors (`auth_type: "ssh"`) and no keys, surface the same prompt alongside the error message.
- Do not block HTTPS/PAT flows; only prompt for SSH clones.
Avoid introducing new dependencies; reuse existing UI components (Dialog, Button, Icon, Tooltip).
  </action>
  <verify>Manually open the clone dialog, enter an SSH URL with no keys, and see the prompt to add a key.</verify>
  <done>Clone flow guides users to add SSH keys when none are registered.</done>
</task>

</tasks>

<verification>
- `./script/generate.ts` updates SDK outputs.
- `bun run build` in `packages/app` succeeds.
</verification>

<success_criteria>

- Settings dialog provides SSH key CRUD via the SDK endpoints.
- Clone dialog prompts for SSH keys when necessary.
- SDK includes updated OpenAPI definitions for SSH key routes.
  </success_criteria>

<output>
After completion, create `.planning/phases/21-ssh-key-management/21-02-SUMMARY.md`
</output>
