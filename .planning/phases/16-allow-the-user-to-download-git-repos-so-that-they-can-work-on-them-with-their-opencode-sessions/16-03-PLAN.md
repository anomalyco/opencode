---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/app/src/components/repo/clone-dialog.tsx
  - packages/app/src/components/repo/repo-selector.tsx
  - packages/app/src/components/repo/repository-manager-dialog.tsx
  - packages/app/src/components/repo/repo-settings-dialog.tsx
  - packages/app/src/hooks/use-clone-progress.ts
  - packages/app/src/pages/home.tsx
  - packages/app/src/components/session/session-new-view.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can clone a repository by URL into the configured workspace root"
    - "User can add an existing local repo path to their available projects"
    - "Clone progress is visible during download"
    - "User can pick a branch before cloning and switch branches later in repo settings"
    - "User is warned before branch switch when working tree is dirty"
  artifacts:
    - path: "packages/app/src/components/repo/clone-dialog.tsx"
      provides: "Clone-by-URL dialog with progress and auth retry"
      exports: ["CloneDialog"]
    - path: "packages/app/src/components/repo/repository-manager-dialog.tsx"
      provides: "Repo list + add local path + open settings"
      exports: ["RepositoryManagerDialog"]
    - path: "packages/app/src/components/repo/repo-selector.tsx"
      provides: "Repo + branch selector for session start"
      exports: ["RepoSelector"]
    - path: "packages/app/src/hooks/use-clone-progress.ts"
      provides: "Clone progress tracking with SSE and retry"
      exports: ["useCloneProgress"]
  key_links:
    - from: "packages/app/src/components/repo/clone-dialog.tsx"
      to: "/repo/clone-progress"
      via: "EventSource/SSE"
      pattern: "clone-progress"
    - from: "packages/app/src/components/repo/repository-manager-dialog.tsx"
      to: "packages/sdk/js/src/gen/**"
      via: "SDK repo list/add"
      pattern: "repo"
    - from: "packages/app/src/components/repo/repo-settings-dialog.tsx"
      to: "/repo/:id/checkout"
      via: "SDK repo checkout/branch switch"
      pattern: "repo\\.(checkout|switch|branch)"
    - from: "packages/app/src/components/repo/repo-selector.tsx"
      to: "/repo/:id/branches"
      via: "SDK repo branches list"
      pattern: "repo\\.branches"
---

<objective>
Integrate the Ralphcity clone workflow into the opencode web UI so users can clone/download repositories, manage repo selection, and switch branches while working in opencode sessions.

Purpose: Users need a first-class UI flow to clone repos (with progress and auth handling) and select them for new sessions, mirroring Ralphcity UI behavior while respecting opencode's UX patterns.
Output: Repo clone and management UI in `packages/app` wired to SDK repo endpoints, with progress, branch selection, and dirty-tree warnings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-CONTEXT.md

# Frontend integration targets (Solid app)

@packages/app/src/pages/home.tsx
@packages/app/src/components/session/session-new-view.tsx
@packages/app/src/context/global-sync.tsx
@packages/app/src/components/dialog-select-directory.tsx

# Ralphcity UI reference (clone workflow + progress semantics)

@packages/ralphcity-ui/frontend/src/components/ralphtown/CloneDialog.tsx
@packages/ralphcity-ui/frontend/src/components/ralphtown/RepositoryManagerDialog.tsx
@packages/ralphcity-ui/frontend/src/components/ralphtown/RepoSelector.tsx
@packages/ralphcity-ui/frontend/src/hooks/useCloneProgress.ts
@packages/ralphcity-ui/frontend/src/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Ralphcity clone workflow into opencode UI</name>
  <files>
    packages/app/src/components/repo/clone-dialog.tsx
    packages/app/src/components/repo/repo-selector.tsx
    packages/app/src/components/repo/repository-manager-dialog.tsx
    packages/app/src/components/repo/repo-settings-dialog.tsx
    packages/app/src/hooks/use-clone-progress.ts
    packages/app/src/pages/home.tsx
    packages/app/src/components/session/session-new-view.tsx
  </files>
  <action>
Build Solid-based UI components that mimic Ralphcity behavior, wired to new repo APIs:

1. **Clone dialog + progress hook**
   - Create `use-clone-progress.ts` patterned on Ralphcity's `useCloneProgress` but using Solid primitives.
   - Use EventSource for `GET /repo/clone-progress` and fetch POST for credential retry.
   - Display progress using existing UI primitives (e.g., `ProgressCircle` + textual stats).
   - Preserve Ralphcity auth retry UX (PAT/HTTPS/SSH) and error help steps.

2. **Repository management UI**
   - Add `RepositoryManagerDialog` to list repos, add local path, and open settings.
   - Add `RepoSelector` for choosing repo and branch when starting a new session.
   - Add `RepoSettingsDialog` for branch switching after clone:
     - Show current branch list.
     - If checkout endpoint reports dirty state, show warning and require confirmation before retrying with `force` or explicit override (per server behavior).

3. **Integrate into opencode pages**
   - On `home.tsx`, add a "Clone repository" action near "Open project"; on success, open project and navigate.
   - In `session-new-view.tsx`, add repo selection + branch picker (pre-clone branch selection when available).
   - Display the configured workspace root (from `sync.data.config.workspace.root`) in clone dialog copy.

4. **UI copy + behavior**
   - Keep microcopy close to Ralphcity UI where possible, but use opencode design tokens.
   - Ensure actions are available to authenticated users with project access.
     </action>
     <verify>
5. Home screen shows both "Open project" and "Clone repository" actions.
6. Clone dialog shows progress and completes by opening the new repo in opencode.
7. Repo selector lets users choose repo + branch for new sessions.
8. Branch switch warning appears when working tree is dirty.
   </verify>
   <done>
   Ralphcity clone workflow is available in opencode UI with progress, branch selection, and repo settings integration.
   </done>
   </task>

</tasks>

<verification>
1. UI can clone by URL and add existing local repos.
2. Branch selection works before and after cloning, with dirty-tree warnings.
3. Clone progress and auth retry flows are visible and functional.
</verification>

<success_criteria>

- User can clone a repo from a URL into the configured workspace root.
- Clone progress is visible and errors show retry/help guidance.
- User can add an existing local repo path to the list.
- Branch selection is available pre-clone and in repo settings, with dirty-tree warnings.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-03-SUMMARY.md`
</output>
