---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/opencode/src/config/config.ts
  - packages/opencode/src/repo/repo.ts
  - packages/opencode/src/server/routes/repo.ts
  - packages/opencode/src/server/server.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Repo list/add/clone/branch endpoints are available on the server"
    - "Clone downloads to a configured workspace root and supports branch selection"
    - "Dirty working tree checks block unsafe branch switches"
    - "Clone/download actions are audit logged with user and repo details"
  artifacts:
    - path: "packages/opencode/src/server/routes/repo.ts"
      provides: "Repo list/add/clone/branch APIs plus progress streaming"
      exports: ["RepoRoutes"]
    - path: "packages/opencode/src/repo/repo.ts"
      provides: "Repo storage, clone helpers, and audit logging"
      contains: "Repo"
    - path: "packages/opencode/src/config/config.ts"
      provides: "Workspace root configuration for clone destination"
      contains: "workspace"
  key_links:
    - from: "packages/opencode/src/server/routes/repo.ts"
      to: "packages/opencode/src/repo/repo.ts"
      via: "Repo namespace helpers"
      pattern: "Repo\\."
    - from: "packages/opencode/src/repo/repo.ts"
      to: "packages/opencode/src/util/log.ts"
      via: "Audit logging"
      pattern: "Log\\.create\\(\\{ service: \"repo\" \\}\\)"
---

<objective>
Add server-side repo clone/list/branch APIs with workspace root configuration and audit logging to support repository download workflows.

Purpose: The UI and SDK depend on a stable backend with clone progress, branch switching, and repo storage; this plan establishes the backend foundation with audit compliance.

Output: Repo clone and management APIs in `packages/opencode`, plus a configurable workspace root and audit logging for clone/download actions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-CONTEXT.md

# Ralphcity backend reference for clone/progress behavior

@packages/ralphcity-ui/backend/src/api/repos.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo clone APIs, workspace root config, and audit logging</name>
  <files>
    packages/opencode/src/config/config.ts
    packages/opencode/src/repo/repo.ts
    packages/opencode/src/server/routes/repo.ts
    packages/opencode/src/server/server.ts
  </files>
  <action>
Add a configurable workspace root and repo/clone API support to the opencode server:

1. **Config: workspace root**
   - Extend `Config.Info` with a `workspace` block (e.g., `{ root?: string }`).
   - Default to a stable location under the user's home (e.g., `~/opencode` or `Global.Path.home` + "/opencode") if unset.
   - Ensure config is returned via `GET /config` so the UI can display the root path.

2. **Repo module**
   - Create `packages/opencode/src/repo/repo.ts` with a `Repo` namespace:
     - Zod schema for repo metadata (id, path, name, created/updated timestamps).
     - Storage helpers using `Storage` (similar to `Project` patterns).
     - Helper to extract repo name from clone URL (mirror Ralphcity behavior).
     - Clone helper that runs `git clone --recurse-submodules` and supports optional branch selection.
     - Dirty check helper (`git status --porcelain`) used before branch switch.
   - Add audit logging via `Log.create({ service: "repo" })` for clone/download actions:
     - Include username/uid if available from auth context, repo URL, destination path, and timestamp.
     - Log both start and completion/failure.

3. **Repo routes**
   - Add `packages/opencode/src/server/routes/repo.ts` using Hono:
     - `GET /repo` -> list repos.
     - `POST /repo` -> add existing local repo path (validate is git repo).
     - `POST /repo/clone` or `GET /repo/clone-progress` (SSE) -> clone with progress events (mirror Ralphcity `clone-progress` semantics with `progress`, `complete`, `clone_error` events).
     - `GET /repo/:id/branches` -> list branches for repo.
     - `POST /repo/:id/checkout` -> switch branch with dirty warning support (return a structured error if uncommitted changes exist).
   - Register the new routes in `packages/opencode/src/server/server.ts` (e.g., `.route("/repo", RepoRoutes())`).

4. **Behavior requirements**
   - Clone includes full `.git` directory and submodules.
   - Clone destination is under the workspace root.
   - Branch selection supported pre-clone (optional `branch` param) and post-clone (checkout endpoint).
   - Dirty working tree warning is returned as a structured error that the UI can render.
     </action>
     <verify>
5. `GET /repo` returns a JSON array and succeeds for authenticated users.
6. `POST /repo/clone` streams progress events and completes with `complete` event on success.
7. `POST /repo/:id/checkout` returns a warning/blocked response when repo is dirty.
8. `GET /config` includes `workspace.root` when set.
   </verify>
   <done>
   Repo clone/list/branch endpoints exist, clone progress streams, workspace root is configurable, and audit logs are emitted for clone/download actions.
   </done>
   </task>

</tasks>

<verification>
1. Repo clone endpoint works end-to-end with progress events.
2. Branch selection works before and after cloning, with dirty-tree warnings.
3. Audit logs exist for clone/download actions.
</verification>

<success_criteria>

- Repo clone/list/branch APIs are available for authenticated users.
- Clone downloads to the configured workspace root with optional branch selection.
- Dirty working tree warnings block unsafe branch switches.
- Clone/download actions are recorded via server audit logs.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-01-SUMMARY.md`
</output>
