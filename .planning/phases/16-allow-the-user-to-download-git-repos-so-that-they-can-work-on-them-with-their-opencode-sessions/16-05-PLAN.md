---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 05
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - packages/app/src/components/session/session-new-view.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Users can open the repository manager from the new session view"
    - "Repository manager entry point is visible even when no repos are listed"
  artifacts:
    - path: "packages/app/src/components/session/session-new-view.tsx"
      provides: "Manage repos action in new session view"
      exports: ["NewSessionView"]
  key_links:
    - from: "packages/app/src/components/session/session-new-view.tsx"
      to: "packages/app/src/components/repo/repository-manager-dialog.tsx"
      via: "Manage repos button toggles dialog"
      pattern: "RepositoryManagerDialog"
---

<objective>
Remove ambiguity around the repository manager entry point by making it accessible from the new session view, not only Home.

Purpose: UAT reported confusion about how to open the repository manager because it only appears on Home. Adding an entry point in the new session view makes the action discoverable in more empty states.
Output: A visible "Manage repos" action in the new session view that opens `RepositoryManagerDialog`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-CONTEXT.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-UAT.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-03-SUMMARY.md

@packages/app/src/components/session/session-new-view.tsx
@packages/app/src/components/repo/repository-manager-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Manage repos entry point in the new session view</name>
  <files>packages/app/src/components/session/session-new-view.tsx</files>
  <action>
Add a "Manage repos" action in `NewSessionView` that opens `RepositoryManagerDialog`, using the same dialog component as Home. Place the action near the repo selector controls so it's discoverable during session creation.
  </action>
  <verify>
1. New session view renders a visible "Manage repos" action.
2. Clicking "Manage repos" opens the repository manager dialog.
  </verify>
  <done>
Repository manager is accessible from the new session view, without needing to navigate to Home.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Ensure Manage repos is visible in empty state</name>
  <files>packages/app/src/components/session/session-new-view.tsx</files>
  <action>
Make sure the "Manage repos" action appears even when there are no repos available (empty selector state). Keep layout consistent with existing button styling in the view.
  </action>
  <verify>
1. With no repos available, the new session view still displays the "Manage repos" action.
  </verify>
  <done>
Users can always find the repository manager entry point in the new session view, regardless of repo state.
  </done>
  </task>

</tasks>

<verification>
1. New session view includes a discoverable repository manager entry point.
2. Repo manager dialog opens from the new session view in both empty and non-empty states.
</verification>

<success_criteria>

- Repository manager is accessible outside Home via the new session view.
- Empty state no longer hides repo management controls.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-05-SUMMARY.md`
</output>
