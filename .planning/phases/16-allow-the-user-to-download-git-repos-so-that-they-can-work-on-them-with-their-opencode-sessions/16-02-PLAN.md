---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/sdk/openapi.json
  - packages/sdk/js/src/gen/**
autonomous: true

must_haves:
  truths:
    - "Frontend can call repo list/add/clone/branches/checkout through the SDK"
  artifacts:
    - path: "packages/sdk/openapi.json"
      provides: "Repo endpoint schemas for SDK generation"
      contains: "/repo"
    - path: "packages/sdk/js/src/gen/**"
      provides: "Generated SDK client with repo methods"
  key_links:
    - from: "packages/sdk/openapi.json"
      to: "packages/sdk/js/src/gen/**"
      via: "SDK generation script"
      pattern: "repo"
---

<objective>
Expose repo endpoints through the generated SDK so the frontend can call the new repo APIs with typed methods.

Purpose: The UI depends on SDK clients for server access; keeping the SDK in sync ensures repo/clone operations are available to the app.
Output: Updated OpenAPI spec and regenerated JS SDK with repo endpoints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-CONTEXT.md

@packages/sdk/openapi.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpenAPI and regenerate the SDK</name>
  <files>
    packages/sdk/openapi.json
    packages/sdk/js/src/gen/**
  </files>
  <action>
Expose the repo/clone endpoints to the frontend through the SDK:

1. Update `packages/sdk/openapi.json` to include the new repo endpoints and schemas:
   - Repo list/add/clone/clone-progress/branches/checkout.
   - Clone progress event schema (progress/complete/error) if represented in the SDK.
2. Regenerate the SDK using `./packages/sdk/js/script/build.ts`.
3. Confirm the generated client exposes `repo` endpoints consistent with new server routes.
   </action>
   <verify>
4. `./packages/sdk/js/script/build.ts` completes without errors.
5. The generated client contains typed methods for repo list/add/clone/branches/checkout.
   </verify>
   <done>
   SDK exposes repo endpoints and types required by the frontend.
   </done>
   </task>

</tasks>

<verification>
1. SDK client includes repo list/add/clone/branches/checkout methods after regeneration.
</verification>

<success_criteria>

- OpenAPI includes repo endpoints and clone progress event schema if applicable.
- Generated SDK exposes repo endpoints required by the frontend.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-02-SUMMARY.md`
</output>
