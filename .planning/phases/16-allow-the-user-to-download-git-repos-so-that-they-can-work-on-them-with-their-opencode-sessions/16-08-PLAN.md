---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 08
type: execute
wave: 4
depends_on: ["03", "07"]
files_modified:
  - packages/app/src/components/repo/repository-manager-dialog.tsx
  - packages/app/src/components/repo/repo-selector.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Choosing Add local opens a directory picker and adds the selected repo."
    - "The repo manager clearly states local paths must exist on the host machine."
    - "Branch selector shows a specific, actionable error when branch loading fails."
  artifacts:
    - path: "packages/app/src/components/repo/repository-manager-dialog.tsx"
      provides: "Add local flow with directory picker and helper copy"
    - path: "packages/app/src/components/repo/repo-selector.tsx"
      provides: "Branch selector error messaging with actionable details"
  key_links:
    - from: "packages/app/src/components/repo/repository-manager-dialog.tsx"
      to: "packages/app/src/components/dialog-select-directory.tsx"
      via: "directory picker integration for Add local"
      pattern: "DialogSelectDirectory|openDirectoryPickerDialog"
    - from: "packages/app/src/components/repo/repo-selector.tsx"
      to: "GET /repo/:id/branches"
      via: "branch fetch error handling"
      pattern: "branches.*error|error.*branches"
---

<objective>
Fix Add local UX confusion and surface branch errors in the repo selector UI.

Purpose: Make local repo adding intuitive and expose branch load failures to users.
Output: Directory picker integration and clearer error copy in repo selector.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/app/src/components/repo/repository-manager-dialog.tsx
@packages/app/src/components/repo/repo-selector.tsx
@packages/app/src/components/dialog-select-directory.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add directory picker for Add local and clarify host-path requirement</name>
  <files>packages/app/src/components/repo/repository-manager-dialog.tsx</files>
  <action>
Replace or augment the manual path input for Add local with a directory picker (DialogSelectDirectory or openDirectoryPickerDialog). Add helper copy that clarifies the path must exist on the host machine running opencode, not the client browser. Ensure the selected directory populates the repo path and the add action works without extra manual typing.
  </action>
  <verify>Open the repo manager, select Add local, and confirm the directory picker opens and the selected path populates the repo add flow.</verify>
  <done>Add local uses a directory picker and the UI text clearly explains the host-machine requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Surface branch list errors in the repo selector</name>
  <files>packages/app/src/components/repo/repo-selector.tsx</files>
  <action>
Update branch list error handling to show a specific, actionable message when /repo/:id/branches fails (use error detail or status from the response). If the backend returns a structured 4xx, map it to guidance like "Repo record invalid or missing path" instead of a generic failure.
  </action>
  <verify>Trigger a branch load failure and confirm the UI surfaces a clear, specific error message.</verify>
  <done>Repo selector shows a meaningful error instead of a generic failure when branch list fails.</done>
</task>

</tasks>

<verification>
Verify Add local uses a directory picker and branch selector shows actionable error details when branches fail to load.
</verification>

<success_criteria>

- Add local opens a directory picker and explains host-machine path requirements.
- Branch selector displays a specific error message when branch loading fails.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-08-SUMMARY.md`
</output>
