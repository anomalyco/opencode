---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 07
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/opencode/src/server/routes/repo.ts
  - packages/opencode/src/repo/repo.ts
  - packages/opencode/src/storage/storage.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Branch list endpoint returns a structured 4xx when repo records are invalid or missing path."
    - "Clone/selector flows no longer see 500s for invalid repo records."
    - "Malformed repo storage data is surfaced as a client error, not an unhandled 500."
  artifacts:
    - path: "packages/opencode/src/repo/repo.ts"
      provides: "Validated repo record loading with clear invalid-state errors"
    - path: "packages/opencode/src/storage/storage.ts"
      provides: "Repo record parsing that classifies malformed data"
    - path: "packages/opencode/src/server/routes/repo.ts"
      provides: "Branches endpoint returns 4xx for invalid repo records"
      exports: ["GET /repo/:id/branches"]
  key_links:
    - from: "packages/opencode/src/server/routes/repo.ts"
      to: "packages/opencode/src/repo/repo.ts"
      via: "repo validation before listBranches"
      pattern: "get\\(id\\).*validate"
    - from: "packages/opencode/src/repo/repo.ts"
      to: "packages/opencode/src/storage/storage.ts"
      via: "repo record parsing/validation"
      pattern: "storage.*repo"
---

<objective>
Close branches endpoint 500s by validating repo records and returning structured 4xx errors.

Purpose: Ensure repo clone and selector flows do not break on invalid or malformed repo data.
Output: Backend validation and error mapping for repo branches endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/opencode/src/server/routes/repo.ts
@packages/opencode/src/repo/repo.ts
@packages/opencode/src/storage/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate repo records and classify malformed data</name>
  <files>packages/opencode/src/repo/repo.ts, packages/opencode/src/storage/storage.ts</files>
  <action>
Add validation for repo records when loading by id: treat missing repo path or malformed record data as a typed invalid/NotFound error rather than throwing generic exceptions. Ensure malformed JSON or missing fields in repo storage are mapped to a client-facing 4xx classification that callers can distinguish from CloneError.
  </action>
  <verify>Exercise repo retrieval with a malformed record and confirm a typed invalid/NotFound error is produced (no generic exception).</verify>
  <done>Repo.get (and related helpers) returns a consistent invalid-record error when repo data is malformed or missing required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Map invalid repo records to 4xx in branches endpoint</name>
  <files>packages/opencode/src/server/routes/repo.ts</files>
  <action>
Update the /repo/:id/branches handler to catch invalid/missing repo records (including missing repo.path) and return structured 4xx responses (400/404) with actionable error codes/messages. Preserve existing CloneError handling, and ensure other unexpected errors remain 500.
  </action>
  <verify>Call /repo/:id/branches with an invalid repo id or record and confirm a 4xx response with structured error body is returned.</verify>
  <done>Branches endpoint no longer returns 500 for invalid repo records; it returns a consistent 4xx response.</done>
</task>

</tasks>

<verification>
Confirm invalid repo records and missing paths return structured 4xx from /repo/:id/branches and no longer surface as 500s.
</verification>

<success_criteria>

- Invalid or malformed repo records return 4xx from /repo/:id/branches with a structured error payload.
- Clone and repo selector flows do not receive 500 when repo records are invalid.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-07-SUMMARY.md`
</output>
