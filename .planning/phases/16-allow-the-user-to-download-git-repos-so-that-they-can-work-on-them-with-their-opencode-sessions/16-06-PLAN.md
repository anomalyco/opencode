---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 06
type: execute
wave: 4
depends_on: ["01", "03"]
files_modified:
  - packages/app/vite.config.ts
  - packages/opencode/src/server/routes/repo.ts
  - packages/app/src/components/repo/repo-selector.tsx
  - packages/app/src/components/repo/repo-settings-dialog.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Repo manager renders without dev console errors for agents/commands list shapes"
    - "Branch list requests return a structured 4xx error instead of a 500 when the repo path is invalid"
    - "Branch selectors show a visible error or retry state when branches cannot load"
  artifacts:
    - path: "packages/app/vite.config.ts"
      provides: "Dev proxy routes for /agent and /command"
      contains: "proxy"
    - path: "packages/opencode/src/server/routes/repo.ts"
      provides: "Repo branches endpoint with CloneError handling"
      contains: "repo.branches"
    - path: "packages/app/src/components/repo/repo-selector.tsx"
      provides: "Branch selector error state and retry action"
      exports: ["RepoSelector"]
    - path: "packages/app/src/components/repo/repo-settings-dialog.tsx"
      provides: "Branch list error state with retry"
      exports: ["RepoSettingsDialog"]
  key_links:
    - from: "packages/app/vite.config.ts"
      to: "http://localhost:4096"
      via: "proxy for /agent and /command"
      pattern: '"/agent"|"/command"'
    - from: "packages/opencode/src/server/routes/repo.ts"
      to: "Repo.CloneError"
      via: "branches endpoint try/catch returning RepoErrorResponse"
      pattern: "CloneError"
    - from: "packages/app/src/components/repo/repo-selector.tsx"
      to: "/repo/:id/branches"
      via: "SDK repo.branches with error surface"
      pattern: "repo\\.branches"
---

<objective>
Close UAT blockers by proxying /agent and /command in dev, handling repo branch errors with structured 4xx responses, and surfacing branch-load failures in the UI so branch selectors are actionable.

Purpose: Current dev flows hit the Vite server for /agent and /command and return HTML, while branch list errors surface as 500s and leave selectors inert. Fixing proxy + error handling restores expected repo manager behavior.
Output: Dev proxy entries for /agent and /command, branches endpoint returns structured 4xx on invalid repo paths, and branch selectors show errors with retry.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-UAT.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-04-SUMMARY.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-05-SUMMARY.md

@packages/app/vite.config.ts
@packages/opencode/src/server/routes/repo.ts
@packages/opencode/src/repo/repo.ts
@packages/app/src/components/repo/repo-selector.tsx
@packages/app/src/components/repo/repo-settings-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proxy /agent and /command to backend in dev</name>
  <files>packages/app/vite.config.ts</files>
  <action>
Add Vite dev proxy entries for `/agent` and `/command` using the same backend target and options as existing API proxies. This ensures SDK calls that fetch agents/commands hit the backend instead of returning HTML from the dev server.
  </action>
  <verify>
1. `packages/app/vite.config.ts` includes proxy entries for `/agent` and `/command` pointing to the backend port.
2. In dev, requests to `/agent` and `/command` return JSON arrays (no HTML shape warnings).
  </verify>
  <done>
Agent and command list calls return expected JSON arrays in dev without console shape errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Return structured 4xx for invalid repo paths in branches endpoint</name>
  <files>packages/opencode/src/server/routes/repo.ts</files>
  <action>
Update the `GET /repo/:repoID/branches` handler to catch `Repo.CloneError` (thrown by `ensureGitRepo` in `Repo.listBranches`) and return a 400 response using the existing `RepoErrorResponse` schema. Keep other errors flowing to the global error handler for 404s. Add the 400 response to the OpenAPI route definition to match the handler behavior.
  </action>
  <verify>
1. Calling `/repo/:id/branches` for a repo whose path is missing or not a git repo returns HTTP 400 with `{ error: { message, ... } }`.
2. Valid repo paths still return a 200 with branch list.
  </verify>
  <done>
Branch list requests never surface as 500s for invalid repo paths; they return a structured 4xx error.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface branch list errors in repo selectors</name>
  <files>
    packages/app/src/components/repo/repo-selector.tsx
    packages/app/src/components/repo/repo-settings-dialog.tsx
  </files>
  <action>
Add explicit error/empty states for branch list failures so the selector doesn't appear inert:

1. **RepoSelector**
   - Track branch list load errors when `repo.branches` fails or returns unexpected data.
   - Display a small inline error message and a retry action near the branch selector when the branch list cannot load.

2. **RepoSettingsDialog**
   - Replace the "Loading branches..." fallback with a conditional that shows an error message + retry action when the branch list fails.
   - Keep the loading state for in-flight requests distinct from error state.
     </action>
     <verify>
3. Force a branch list error (invalid repo path or backend down) and confirm both selectors show an error with retry instead of doing nothing.
4. Retry triggers a branch list refetch and restores the selector once branches load.
   </verify>
   <done>
   Branch selectors show actionable error states when branches cannot be loaded, removing the silent failure from UAT.
   </done>
   </task>

</tasks>

<verification>
1. Repo manager opens without "Unexpected agent/command list shape" console errors in dev.
2. `/repo/:id/branches` returns 400 with RepoErrorResponse for invalid repo paths.
3. Branch selectors show error + retry instead of a no-op when branches fail to load.
</verification>

<success_criteria>

- Dev proxy forwards `/agent` and `/command` to the backend, eliminating HTML responses for SDK lists.
- Branch list errors are handled as structured 4xx responses and surfaced in the UI.
- Branch selectors are responsive and informative even when branches cannot load.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-06-SUMMARY.md`
</output>
