---
phase: 16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - packages/app/vite.config.ts
  - packages/app/src/components/repo/repo-selector.tsx
  - packages/app/src/components/dialog-select-directory.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Add local shows directory results instead of an empty list in dev"
    - "Clone progress no longer 404s in dev for public or private repos"
    - "Repo selector surfaces an error when repo list fails to load"
  artifacts:
    - path: "packages/app/vite.config.ts"
      provides: "Dev proxy routes for /repo and /find"
      contains: "proxy"
    - path: "packages/app/src/components/repo/repo-selector.tsx"
      provides: "Repo list error surface with retry"
      exports: ["RepoSelector"]
    - path: "packages/app/src/components/dialog-select-directory.tsx"
      provides: "Directory picker error state when find.files fails"
      exports: ["DialogSelectDirectory"]
  key_links:
    - from: "packages/app/vite.config.ts"
      to: "http://localhost:4096"
      via: "proxy for /repo and /find"
      pattern: '"/repo"|"/find"'
    - from: "packages/app/src/components/dialog-select-directory.tsx"
      to: "/find/file"
      via: "sdk.client.find.files"
      pattern: "find\\.files"
    - from: "packages/app/src/components/repo/repo-selector.tsx"
      to: "/repo"
      via: "globalSDK.client.repo.list"
      pattern: "repo\\.list"
---

<objective>
Close dev/UAT blockers by proxying repo and find routes in Vite and surfacing repo-list/find-file errors in the UI so empty states are actionable.

Purpose: The repo selector and directory picker are blocked by missing dev proxy routes and silent failures; this restores expected responses and makes failures visible.
Output: Vite proxy routes for `/repo` and `/find`, plus visible error states and retries in repo selector and directory picker.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-CONTEXT.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-UAT.md
@.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-03-SUMMARY.md

@packages/app/vite.config.ts
@packages/app/src/components/dialog-select-directory.tsx
@packages/app/src/components/repo/repo-selector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proxy /repo and /find to backend in dev</name>
  <files>packages/app/vite.config.ts</files>
  <action>
Add Vite dev proxy entries for `/repo` and `/find` that mirror the existing API proxy targets (same backend host/port and changeOrigin settings). This ensures clone progress and find.files requests reach the backend when `server.url` is `window.location.origin` in dev.
  </action>
  <verify>
1. `packages/app/vite.config.ts` proxy table includes `/repo` and `/find` entries targeting the backend port.
2. In dev, `GET http://localhost:3001/repo/clone-progress?...` and `GET http://localhost:3001/find/file?...` no longer 404 (expect backend responses or SSE).
  </verify>
  <done>
Dev proxy forwards `/repo` and `/find` to the backend so repo list, clone progress, and directory search reach server routes.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Surface repo list and directory picker failures</name>
  <files>
    packages/app/src/components/repo/repo-selector.tsx
    packages/app/src/components/dialog-select-directory.tsx
  </files>
  <action>
Add explicit error handling so users see actionable feedback instead of empty lists:

1. **Repo selector**
   - Track repo list load errors (network or non-array response).
   - Display an inline error message with a retry action that triggers `refetch`.
   - Keep the existing successful path unchanged.

2. **Directory picker**
   - When `find.files` fails or returns an unexpected shape, show an empty-state error message instead of a blank list.
   - Provide a short hint to check server connectivity or proxy configuration.
     </action>
     <verify>
3. Simulate repo list failure (disconnect backend or force error) and confirm the repo selector shows a visible error plus retry.
4. Trigger `Add local` with backend offline and confirm the directory picker shows an error message instead of an empty list.
   </verify>
   <done>
   Repo list and directory search failures are visible to users and include a retry/next-step hint, preventing silent empty states.
   </done>
   </task>

</tasks>

<verification>
1. Add local directory picker populates when backend is available.
2. Clone progress connects in dev without 404s.
3. Repo selector shows a visible error when repo list fails.
</verification>

<success_criteria>

- Dev proxy forwards `/repo` and `/find` to the backend.
- Add local directory picker and clone progress work in dev.
- Repo selector and directory picker show clear error states instead of silent empty lists.
  </success_criteria>

<output>
After completion, create `.planning/phases/16-allow-the-user-to-download-git-repos-so-that-they-can-work-on-them-with-their-opencode-sessions/16-04-SUMMARY.md`
</output>
