# OpenCode 开发环境

## 快速启动

### Web 开发模式

**方式1：使用启动脚本**
- 双击 `start-all.bat` - 同时启动前后端服务
- 双击 `start-backend.bat` - 仅启动后端（9999端口）
- 双击 `start-frontend.bat` - 仅启动前端（3000端口）

**方式2：手动启动**

终端1 - 启动后端（9999端口）：
```bash
cd packages/opencode
bun run ./src/index.ts serve --hostname 0.0.0.0 --port 9999
```

终端2 - 启动前端（3000端口）：
```bash
cd packages/app
bun run dev
```

### 桌面应用模式

**开发模式（Tauri 窗口）：**

1. 先启动后端服务（见上方）
2. 双击 `run-desktop-dev.bat` 启动桌面应用

**构建独立客户端：**

双击 `build-desktop.bat` 构建安装程序

输出文件：`packages/desktop/src-tauri/target/release/ZFlow_1.1.36_x64-setup.nsi`

详细说明：见 [ZFlow桌面应用.md](ZFlow桌面应用.md)

### 访问地址

- Web前端：<http://localhost:3000>
- 桌面应用：Tauri 窗口
- 后端API：<http://localhost:9999>

## 二次开发功能

已开发的功能（在 packages/app/src 中）：

- BrowserPanel - 浏览器面板组件
- HTMLPreview - HTML预览组件
- McpDashboard - MCP仪表板
- settings-skills - 技能设置
- TaskView - 任务视图页面

## 开发流程

1. Web开发：在浏览器访问 <http://localhost:3000>
2. 桌面应用：使用 Tauri 窗口
3. 修改 packages/app/src 中的代码会自动热更新
4. 修改后端需重启后端服务

---

旧命令（仅供参考）：

启动前段服务：PS F:\pythonproject\opencode> bun run --cwd packages/app dev
$env:LOCAL_FRONTEND=1; bun run ./src/index.ts serve --hostname 0.0.0.0 --port 9999

