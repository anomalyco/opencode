---
description: Export analysis output to an Obsidian vault
---

Use the `mystery-command-workflow` skill guidance.

Execute `/export obsidian`.

Input:
- `$ARGUMENTS` may include target and options.
