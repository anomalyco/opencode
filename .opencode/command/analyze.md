---
description: Analyze selected chapters and update structured outputs
---

Use the `mystery-command-workflow` skill guidance.

Execute `/analyze` with optional arguments:
- `--only people|events|clues|foreshadows|threads|hypotheses`
- `--force`

Input:
- `$ARGUMENTS` contains optional flags.
