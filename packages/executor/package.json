{
  "name": "@veritly/executor",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun run src/test.ts",
    "test:integration": "bun run --cwd ../opencode test:executor-sdk",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "1.3.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
