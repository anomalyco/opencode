// Italian UI strings — placeholder. Untranslated keys fall back to the
// English dictionary via the loader merge in packages/app/src/context/language.tsx.
// Native Italian speakers welcome to flesh this out as a follow-up.
export const dict: Record<string, string> = {}
