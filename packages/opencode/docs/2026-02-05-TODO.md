# 📋 OpenCode — 종합 TODO 보고서

**일자**: 2026-02-05
**분석 범위**: `packages/opencode/` 전체 코드베이스
**분석 방법**: 코드 품질/테스트, 아키텍처/성능, 보안/DevOps, 기능 완성도/DX — 4개 병렬 분석
**기존 보고서 참조**: `2026-02-04-unimplemented-features-report.md`, `2026-02-05-security-audit-report.md`

---

## 📊 현황 요약

| 영역            | 완성도 | 품질       | 긴급도 |
| --------------- | ------ | ---------- | ------ |
| ACP 프로토콜    | 10/10  | ⭐⭐⭐⭐⭐ | —      |
| 세션 관리       | 9/10   | ⭐⭐⭐⭐   | 🟡     |
| 에이전트 시스템 | 10/10  | ⭐⭐⭐⭐⭐ | —      |
| CLI 명령어      | 10/10  | ⭐⭐⭐⭐   | —      |
| LSP 통합        | 10/10  | ⭐⭐⭐⭐⭐ | —      |
| 스냅샷/언두     | 10/10  | ⭐⭐⭐⭐   | —      |
| TUI             | 9/10   | ⭐⭐⭐⭐   | 🟢     |
| 플러그인 시스템 | 7/10   | ⭐⭐⭐     | 🟡     |
| 파일 감시       | 7/10   | ⭐⭐⭐     | 🟡     |
| 보안            | 7/10   | ⭐⭐⭐     | 🔴     |
| 테스트 커버리지 | 4/10   | ⭐⭐       | 🔴     |
| 문서화          | 5/10   | ⭐⭐       | 🟡     |
| DevOps          | 6/10   | ⭐⭐⭐     | 🟡     |

---

## 🔴 P0 — Critical (즉시 수행)

### C-01. 테스트 커버리지 확대 — 핵심 모듈 무테스트

**현황**: src/ 150+개 파일 중 테스트 존재 ~48개 (약 32%)

| 모듈                       | 추정 커버리지 | 위험도 | 비고                                           |
| -------------------------- | ------------- | ------ | ---------------------------------------------- |
| `src/storage/storage.ts`   | **0%**        | 🔴     | 모든 세션/설정 데이터가 의존하는 영속성 레이어 |
| `src/auth/index.ts`        | **0%**        | 🔴     | 인증 모듈 — 9개 이상 모듈에서 참조             |
| `src/session/processor.ts` | **0%**        | 🔴     | 핵심 처리 로직                                 |
| `src/session/prompt.ts`    | **~5%**       | 🔴     | 1847줄 God Module — 앱 전체의 핵심 루프        |
| `src/server/routes/`       | **12%**       | 🔴     | 16개 라우트 파일 중 2개만 테스트               |
| `src/mcp/index.ts`         | **0%**        | 🟡     | MCP 클라이언트 핵심 로직 (~700줄)              |
| `src/provider/auth.ts`     | **0%**        | 🟡     | 프로바이더 인증                                |
| `src/bus/`                 | **0%**        | 🟡     | 이벤트 버스 인프라                             |
| `src/cli/` (대부분)        | **7%**        | 🟡     | 40+개 소스 중 3개만 테스트                     |

**작업 항목**:

- [ ] `storage.ts` 유닛 테스트 작성 (CRUD, list, concurrent access)
- [ ] `auth/index.ts` 인증 플로우 테스트 작성
- [ ] `session/processor.ts` 처리 루프 테스트 작성
- [ ] `server/routes/` 최소 config, global, mcp, permission 라우트 테스트
- [ ] `mcp/index.ts` 클라이언트 연결/도구 발견 테스트

### C-02. 보안 — 서버 미들웨어 부재

**파일**: `src/server/server.ts`

- [ ] **요청 본문 크기 제한**: Hono `bodyLimit` 미들웨어 추가 (10MB 제한 권장)
- [ ] **보안 헤더 추가**: `X-Content-Type-Options`, `X-Frame-Options`, `Strict-Transport-Security`, `Cache-Control`
- [ ] **Rate limiting**: 최소 `/log`, `/auth` 엔드포인트에 적용
- [ ] **네트워크 노출 경고**: `hostname`이 loopback이 아닐 때 인증 없으면 경고 출력

### C-03. 보안 — 플러그인 권한 시스템 우회

**파일**: `src/tool/registry.ts:60-82`

- [ ] 플러그인 도구 실행에 `ctx.ask()` 권한 체크 래핑 추가
- [ ] 플러그인 신뢰 모델 문서화

### C-04. CI/CD — 의존성 취약점 스캔 없음

**파일**: `.github/workflows/test.yml`

- [ ] CI에 `bun audit` 또는 Snyk/Dependabot 스텝 추가
- [ ] `publish.yml`에서 `actions/checkout@v3` → `@v4` 업그레이드

---

## 🟡 P1 — High (1~2주 내 수행)

### H-01. 아키텍처 — `SessionPrompt` God Module 분해

**파일**: `src/session/prompt.ts` (1847줄, 30+ 모듈 임포트)

| 추출 대상           | 현재 위치                              | 예상 크기 |
| ------------------- | -------------------------------------- | --------- |
| Tool Context 팩토리 | 3곳에서 중복 생성 (L664, L1029, L1092) | ~50줄     |
| MCP 도구 래핑 로직  | L695-830                               | ~135줄    |
| 쉘 명령 실행        | L896-950                               | ~55줄     |
| 타이틀 생성         | L1740-1830                             | ~90줄     |
| 메시지 클론/직렬화  | L560-600                               | ~40줄     |

- [ ] `Tool.Context` 팩토리 함수 추출
- [ ] MCP 도구 래핑 → `src/session/mcp-tools.ts` 분리
- [ ] 타이틀 생성 → `src/session/title.ts` 분리
- [ ] TODO: "centralize invoke tool logic" (L320) 해결

### H-02. 아키텍처 — `server.ts` 라우트 파일 분리

**파일**: `src/server/server.ts` (L60 TODO 명시됨)

현재 인라인 라우트: `/auth`, `/instance/dispose`, `/path`, `/vcs`, `/command`, `/log`, `/agent`, `/skill`, `/lsp`, `/formatter`, `/event`, 프록시

- [ ] 인라인 라우트들을 `src/server/routes/` 하위 파일로 추출
- [ ] `as unknown as Hono` 캐스팅 (L548) 제거
- [ ] SSE 하트비트 간격을 설정 가능하게 변경

### H-03. 권한 시스템 — 디스크 영속화

**파일**: `src/permission/next.ts:223`
**현황**: "always allow" 규칙이 메모리에만 저장, 재시작 시 소실

- [ ] 권한 규칙셋 디스크 저장 구현
- [ ] 규칙 관리 UI (TUI 다이얼로그) 추가
- [ ] 권한 가져오기/내보내기 기능

### H-04. 성능 — 세션 목록 조회 최적화

**파일**: `src/session/index.ts:332-339`, `src/storage/storage.ts:212-226`

**현황**: `Session.list()`가 모든 세션 파일을 풀 스캔, `Session.children()`도 전체 세션 읽기 후 필터

- [ ] Storage 레이어에 인덱스 또는 메타데이터 캐시 추가
- [ ] 페이지네이션 지원
- [ ] `children()` 부모 ID 인덱스

### H-05. 성능 — 무한 증가하는 캐시

| 캐시              | 파일                           | 문제                                              |
| ----------------- | ------------------------------ | ------------------------------------------------- |
| SDK Map           | `src/provider/provider.ts:717` | 프로바이더 옵션 조합마다 SDK 생성, 퇴거 정책 없음 |
| LanguageModel Map | `src/provider/provider.ts:718` | 모델마다 항목 추가, 삭제 없음                     |
| FileTime.read     | `src/file/time.ts`             | 세션별 파일 읽기 시간 무한 누적                   |

- [ ] SDK/LanguageModel 캐시에 LRU 또는 크기 제한 추가
- [ ] `FileTime.state` 세션 종료 시 정리 로직 추가

### H-06. 에러 처리 — Silent Catch 패턴 정리

**현황**: `.catch(() => {})` 58건, `.catch(() => undefined)` 29건, `.catch(() => "")` 8건

**우선 정리 대상** (의미 있는 에러를 삼키는 경우):

| 파일                            | 줄                                                   | 내용 |
| ------------------------------- | ---------------------------------------------------- | ---- |
| `src/config/config.ts:273`      | `BunProc.run().catch(() => {})` — 플러그인 설치 실패 |
| `src/config/config.ts:284`      | `BunProc.run().catch(() => {})` — 의존성 설치 실패   |
| `src/config/config.ts:1319`     | `catch (err) {}` — 플러그인 해석 실패                |
| `src/cli/cmd/import.ts:70`      | `file.json().catch(() => {})` — 임포트 데이터 파싱   |
| `src/lsp/server.ts:97,172,1084` | `Bun.resolve().catch(() => {})` — LSP 서버 해석      |

- [ ] 최소 `log.debug()` 추가하여 에러 가시성 확보
- [ ] 빈 `catch (err) {}` 블록 2건 → 로깅 추가

### H-07. 문서화 — README.md 교체 및 API 문서

**현황**: `packages/opencode/README.md`가 "bun init" 보일러플레이트 상태

- [ ] README.md → 프로젝트 소개, 설치, 사용법, 아키텍처 개요 작성
- [ ] Zod 스키마 기반 API 문서 자동 생성 검토
- [ ] 환경 변수 문서 (Flag 네임스페이스 기반 `.env.example` 생성)

---

## 🟢 P2 — Medium (한 달 내 수행)

### M-01. 코드 품질 — @ts-ignore / @ts-expect-error 정리

**현황**: 총 11건 (설명 없는 것 4건)

| 파일                                    | 유형               | 설명                  | 조치                          |
| --------------------------------------- | ------------------ | --------------------- | ----------------------------- |
| `src/file/watcher.ts:9`                 | `@ts-ignore`       | 설명 없음             | 타입 선언 추가 또는 주석 보완 |
| `src/session/llm.ts:250`                | `@ts-expect-error` | 설명 없음             | 근본 원인 파악 및 수정        |
| `src/cli/cmd/tui/context/helper.tsx:13` | `@ts-expect-error` | 설명 없음             | 수정                          |
| `src/cli/cmd/tui/context/theme.tsx:364` | `@ts-expect-error` | 설명 없음             | 수정                          |
| `src/plugin/index.ts:108,124`           | `@ts-expect-error` | SDK v2 미이전         | SDK v2 마이그레이션           |
| `src/provider/provider.ts:79`           | `@ts-ignore`       | "kill this code" TODO | 레거시 코드 제거              |
| `src/provider/provider.ts:1041`         | `@ts-ignore`       | Bun 버그 우회         | Bun 업그레이드 시 재확인      |

- [ ] 설명 없는 4건 즉시 주석 또는 수정
- [ ] 플러그인 SDK v2 마이그레이션 (2건 해결)
- [ ] 레거시 코드 제거 (provider.ts:79)

### M-02. 코드 품질 — 죽은 코드 제거

| 파일                             | 문제                                              | 조치                           |
| -------------------------------- | ------------------------------------------------- | ------------------------------ |
| `src/session/message.ts` (190줄) | `message-v2.ts`로 대체됨, 어디서도 미사용         | 삭제                           |
| `src/session/system.ts:41`       | `project.vcs === "git" && false` — 도달 불가 코드 | `&& false` 제거 또는 기능 복구 |
| `src/plugin/copilot.ts:43-52`    | 주석 처리된 대형 코드 블록                        | 정리                           |
| `src/wrapper.ts`                 | 미추적 신규 파일 — 용도 확인 필요                 | 확인 후 삭제/통합              |

### M-03. 코드 품질 — TODO/FIXME 정리 (21건)

**상위 우선순위 TODO**:

| 파일                                  | 줄                                         | TODO 내용     | 우선순위 |
| ------------------------------------- | ------------------------------------------ | ------------- | -------- |
| `src/session/prompt.ts:320`           | "centralize invoke tool logic"             | HIGH          |
| `src/session/prompt.ts:1728`          | "make task tool accept complex input"      | MEDIUM        |
| `src/provider/provider.ts:200,385`    | "Env.set only updates shallow copy" (2건)  | MEDIUM        |
| `src/provider/transform.ts:363`       | "CANNOT SET max_tokens if this is set"     | MEDIUM        |
| `src/provider/sdk/copilot/chat/*:374` | "lost type safety on Chunk. MUST FIX"      | HIGH          |
| `src/permission/next.ts:223`          | "don't save ruleset to disk yet"           | HIGH (→ H-03) |
| `src/server/server.ts:60`             | "Break server.ts into smaller route files" | HIGH (→ H-02) |
| `src/tool/bash.ts:54`                 | "rename this tool for other shells"        | LOW           |
| `src/config/config.ts:267,280`        | Bun 버그 #19936 우회 (2건)                 | LOW           |
| `src/cli/cmd/github.ts:205`           | "add guide for copilot"                    | LOW           |

### M-04. 코드 품질 — 디버그 console.log 제거 (~15건)

| 파일                                        | 줄                   | 내용                                                               |
| ------------------------------------------- | -------------------- | ------------------------------------------------------------------ |
| `src/cli/cmd/tui/util/clipboard.ts`         | 79,88,97,110,125,149 | `console.log("clipboard: using ...")`                              |
| `src/cli/cmd/tui/context/sync.tsx:332`      |                      | `console.log("bootstrapping")`                                     |
| `src/cli/cmd/tui/context/route.tsx:34`      |                      | `console.log("navigate", route)`                                   |
| `src/cli/cmd/tui/context/theme.tsx:319,325` |                      | `console.log("resolveSystemTheme")`, `console.log(colors.palette)` |
| `src/cli/cmd/tui/app.tsx:214`               |                      | `console.log(JSON.stringify(route.data))`                          |
| `src/server/routes/session.ts:119`          |                      | `log.info("SEARCH", ...)` — 디버그 로그                            |

- [ ] `console.log` → `Log.create()` 패턴으로 전환 또는 삭제

### M-05. DevOps — Graceful Shutdown 완성

**파일**: `src/index.ts:154-158`, `src/server/server.ts`

**현황**: SIGTERM/SIGINT 핸들러 없음, SSE 하트비트 정리 안 됨

- [ ] SIGTERM 핸들러 추가: 새 연결 거부 → 활성 요청 대기 → SSE 스트림 종료 → Instance dispose → 종료
- [ ] SSE 하트비트 인터벌을 서버 stop() 시 정리
- [ ] 활성 연결 드레인 타임아웃 설정

### M-06. DevOps — Docker 프로덕션 강화

**파일**: `Dockerfile` (루트 + 패키지)

- [ ] 프로덕션 `Dockerfile`에 non-root 유저 추가
- [ ] `HEALTHCHECK` 추가
- [ ] `.dockerignore` 파일 생성 (`.git`, `node_modules`, `test/`, `*.log`, `.env*` 제외)
- [ ] Alpine 버전 고정

### M-07. DevOps — 모니터링/관측성

- [ ] `/metrics` 엔드포인트 추가 (요청 수, 레이턴시, 활성 세션, 에러율)
- [ ] OpenTelemetry를 HTTP 요청, 도구 실행, MCP 호출에 확장
- [ ] 선택적 에러 추적 통합 (Sentry 등) 검토

### M-08. 성능 — 메시지 Deep Clone 최적화

**파일**: `src/session/prompt.ts:579`

**현황**: 루프 매 반복마다 전체 메시지 이력 deep clone → 대화 길이에 비례하여 CPU/메모리 증가

- [ ] 불변 데이터 구조 도입 또는 COW(Copy-on-Write) 패턴 적용
- [ ] 구조적 공유(structural sharing) 검토

### M-09. 프로바이더 — 하드코딩된 프로바이더별 로직

**파일**: `src/provider/transform.ts`

**현황**: `normalizeMessages`, `variants`, `options` 등이 프로바이더 ID/모델명 문자열 매칭으로 거대한 switch/if-else 체인

- [ ] 프로바이더별 설정을 데이터 기반으로 전환 (models.dev 프리셋 활용)
- [ ] Kimi 모델명 우회코드 제거 (transform.ts:338 — upstream 수정 후)
- [ ] `max_tokens` 충돌 해결 (transform.ts:363)

### M-10. MCP — 에러 로그 메시지 오류

**파일**: `src/mcp/index.ts:237`

- [ ] `"failed to get prompts"` → `"failed to get resources"` 수정 (복사-붙여넣기 오류)

---

## 🔵 P3 — Low (백로그)

### L-01. 테스트 품질 개선

| 테스트 파일                 | 문제                                                 |
| --------------------------- | ---------------------------------------------------- |
| `test/bun.test.ts`          | 소스코드 텍스트를 문자열로 검증 — 동작 테스트가 아님 |
| `test/file/ignore.test.ts`  | 단일 테스트 케이스만 (`node_modules` 패턴)           |
| `test/util/timeout.test.ts` | 2개 테스트만 — 취소, 제로 타임아웃 미테스트          |
| `test/file/ripgrep.test.ts` | 숨김 파일 포함/제외만 테스트                         |

### L-02. 보안 — 추가 강화

- [ ] `.gitignore`에 `.env`, `.env.local`, `*.pem`, `*.key` 패턴 추가
- [ ] `OPENCODE_CONFIG_CONTENT` JSON.parse에 try/catch 추가 (config.ts:177)
- [ ] `OPENCODE_PERMISSION` 파싱에 Zod 검증 추가 (config.ts:202)
- [ ] 코드검색/웹검색 SSE 데이터 `JSON.parse`에 try/catch 추가 (codesearch.ts:105, websearch.ts:123)
- [ ] MCP 환경변수 패스스루 필터링 또는 문서화 (mcp/index.ts:416-420)
- [ ] OAuth 토큰 OS 키체인 통합 검토

### L-03. 플러그인 시스템 성숙화

- [ ] 플러그인 SDK v2 마이그레이션 완료
- [ ] 플러그인 라이프사이클 훅 (unload/reload)
- [ ] 플러그인 디스커버리/마켓플레이스 기능
- [ ] Copilot Messages API 재활성화 (rate limit 해소 시)

### L-04. 파일 감시 개선

- [ ] 전체 디렉토리 감시를 기본 활성화 (현재 실험적 플래그 뒤)
- [ ] 와처 사망 시 재연결 로직 추가
- [ ] `@ts-ignore` 제거 (watcher.ts:9 — parcel watcher 타입)

### L-05. 코드 중복 제거

- [ ] Worktree porcelain 파싱 중복 (worktree/index.ts — `remove`와 `reset`에서 동일 코드)
- [ ] GitHub Copilot Enterprise 프로바이더 로직 중복 (provider.ts:724-735, 860-898)
- [ ] "Ask Permission Then Execute" 패턴 공유 미들웨어화

### L-06. `any` 타입 사용 감소

**현황**: src/ 전체에 50+ 건의 `any` 타입 사용

- [ ] 우선순위: 공개 API/익스포트에서의 `any` → 구체 타입으로 변환
- [ ] `Tool.Context.extra: { [key: string]: any }` → 타입 안전하게 개선

### L-07. Bash 도구 크로스-쉘 이름 변경

**파일**: `src/tool/bash.ts:54`

- [ ] "bash" → "shell" 또는 "terminal" 등 쉘 불문 이름 검토
- [ ] zsh, fish, nu, PowerShell 지원 이미 구현됨 — 이름만 미반영

### L-08. 세션 관련 소소한 개선

- [ ] 더 이상 사용하지 않는 `SessionStatus.Event.Idle` deprecated 이벤트 제거 (status.ts:36)
- [ ] 추론 토큰 가격 모델 개선 (session/index.ts:483)
- [ ] `Env.set` vs `process.env` 직접 사용 불일치 해결 (provider.ts:200,385)

---

## 📈 측정 지표

### 현재 코드베이스 통계

| 지표                          | 값               |
| ----------------------------- | ---------------- |
| src/ TypeScript 파일 수       | ~150+            |
| test/ 테스트 파일 수          | ~48              |
| 추정 테스트 커버리지          | ~32% (파일 기준) |
| TODO/FIXME 주석               | 21건             |
| @ts-ignore + @ts-expect-error | 11건             |
| .catch(() => {}) 패턴         | 58건             |
| Dead code 파일                | 1건 (message.ts) |
| console.log 디버그            | ~15건            |
| 미커밋 변경 파일              | 22건             |

### 목표 (2주 후)

| 지표                             | 목표                          |
| -------------------------------- | ----------------------------- |
| 테스트 커버리지                  | 50%+ (핵심 모듈)              |
| @ts-ignore/@ts-expect-error      | 6건 이하 (Bun 버그 우회 제외) |
| 설명 없는 @ts-expect-error       | 0건                           |
| silent .catch(() => {}) (고위험) | 0건                           |
| TODO 주석                        | 15건 이하                     |
| 보안 미들웨어                    | body limit + headers 적용     |

---

## 🗓️ 권장 실행 순서

```
Week 1: P0 (Critical)
├── C-02 서버 보안 미들웨어 (body limit, headers)
├── C-03 플러그인 권한 체크 추가
├── C-04 CI 의존성 스캔 추가
└── C-01 시작: storage, auth 테스트 작성

Week 2: P0 완료 + P1 시작
├── C-01 완료: processor, server routes 테스트
├── H-01 SessionPrompt 분해 시작
├── H-03 권한 영속화
└── H-06 Silent catch 정리

Week 3-4: P1 완료
├── H-02 server.ts 라우트 분리
├── H-04 세션 목록 성능 최적화
├── H-05 캐시 크기 제한
└── H-07 README 및 문서화

Month 2: P2
├── M-01~M-04 코드 품질 개선
├── M-05~M-07 DevOps 강화
└── M-08~M-10 성능 및 프로바이더 개선

Backlog: P3
└── L-01~L-08 점진적 개선
```

---

## 📎 부록: 미커밋 변경 사항 (현재 워킹 트리)

이전 세션에서 적용한 보안 수정 및 코드 품질 개선이 아직 커밋되지 않았습니다:

```
 D 2026-02-04-unimplemented-features-report.md  → docs/ 이동
 M Dockerfile.railway                           — non-root 유저 추가
 M package.json                                 — junk 필드/스크립트 제거
 M src/mcp/oauth-callback.ts                    — XSS 수정
 M src/cli/cmd/debug/agent.ts                   — code injection 수정
 M src/provider/provider.ts                     — create* guard + type fix
 M src/provider/models.ts                       — JSON.parse 에러 핸들링
 M src/server/routes/session.ts                 — .catch() 추가
 M src/session/prompt.ts                        — abortSignal 안전 처리
 M src/tool/webfetch.ts                         — SSRF 보호
 M src/tool/write.ts                            — TOCTOU race condition 수정
 + src/vendor.d.ts                              — 글로벌 타입 선언
 + docs/2026-02-05-security-audit-report.md     — 보안 감사 보고서
```

**⚠️ 권장**: 이 변경사항들을 먼저 커밋한 후 P0 작업을 시작하세요.

---

_이 보고서는 4개 병렬 분석 에이전트(코드 품질/테스트, 아키텍처/성능, 보안/DevOps, 기능 완성도/DX)의 결과를 종합하여 작성되었습니다._
