research
dist
dist-*
gen
!src/local/llama-skein/gen/
!src/local/llama-skein/gen/**
app.log
script/build-*.ts
# Fork-specific and required: package.json's build:llama-skein-client points
# here. The rule above silently swallowed it, so the command was documented in
# CLAUDE.md but never actually runnable.
!script/build-llama-skein-client.ts
temporary-*.md
.artifacts

# Generated by script/httpapi-exercise.ts (and left behind by local runs)
config.json
