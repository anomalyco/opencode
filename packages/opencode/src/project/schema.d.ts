import { Schema } from "effect";
declare const projectIdSchema: Schema.brand<Schema.String, "ProjectID">;
export type ProjectID = typeof projectIdSchema.Type;
export declare const ProjectID: never;
export {};
