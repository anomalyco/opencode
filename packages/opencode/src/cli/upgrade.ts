import { Bus } from "@/bus"
import { Config } from "@/config/config"
import { Flag } from "@/flag/flag"

export async function upgrade() {
  // Upgrade functionality disabled - version managed by deployment
  return
}
