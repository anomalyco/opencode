Specialized agent for upgrading project dependencies and fixing resulting breaking changes or type errors. Use this when a major library version bump is required (eg. "upgrading Drizzle ORM to v1.0"). specify the target dependency and the desired version. specify the desired safety level: "safe" for automated fixes of common breaking changes, or "thorough" for manual-style refactoring of complex API changes.
