Specialized agent for identifying performance bottlenecks in user flows. Use this when you need to correlate slow UI frames or high latency to specific source code locations. When calling this agent, specify the flow to analyze (eg. "Opening the task dashboard") and the target environment (eg. "development environment"). specify the desired precision: "low" for general hotspots, "medium" for function-level analysis, or "high" for line-by-line performance tracing.
