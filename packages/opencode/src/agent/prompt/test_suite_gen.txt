Specialized agent for generating comprehensive unit and integration test suites for new or existing modules. Use this when you need to ensure 100% test coverage or verify complex logical branches. specify the desired coverage target (eg. "100% path coverage") and the preferred testing framework (eg. "Bun Test"). specify the desired isolation level: "shallow" for unit tests with mocks, or "deep" for integration tests with real dependencies.
