import { Schema } from "effect";
declare const permissionIdSchema: Schema.brand<Schema.String, "PermissionID">;
export type PermissionID = typeof permissionIdSchema.Type;
export declare const PermissionID: never;
export {};
