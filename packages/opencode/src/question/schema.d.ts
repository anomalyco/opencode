import { Schema } from "effect";
declare const questionIdSchema: Schema.brand<Schema.String, "QuestionID">;
export type QuestionID = typeof questionIdSchema.Type;
export declare const QuestionID: never;
export {};
