Open recruiter intake for a req under `.moks/reqs/<slug>` at `${path}`.

This is hiring intake, not a coding AGENTS.md setup. (`/init-code` / `build` is the coding escape hatch.)

Slug: `${slug}`
Req path: `${req}`
Existing reqs ( @ these slugs ): ${existing}

User notes (honor these; they are not a finished JD):
$ARGUMENTS

Stubs already exist if they were missing or empty:
- `${req}/jd.md`
- `${req}/scorecard.md`
- `${req}/notes.md`
- `${req}/scores/.gitkeep`

If `${slug}` is `<slug>` or looks like a placeholder (numeric like `00001`, not a role title), ask for the title. Keep an existing folder — do not create directories when stubs already exist at `${req}`. Do not overwrite non-empty user content. Do not touch other reqs.

## Intake

Talk like a recruiter taking a req from a hiring manager. Write only what they confirmed. Leave the rest TBD.

After each answer, update the stubs, then use the question tool for the next 2–4 gaps. Keep going until title, level, team/HM, location, and must-haves are real. Do not invent a polished JD from a title. Do not stop after the title.

Ask about, in roughly this order:
- Role title
- Level / family (IC vs manager; mid / senior / staff)
- Team, org, hiring manager
- Location / remote
- Must-haves vs nice-to-haves vs deal-breakers
- Comp band / headcount / start if they know
- Interview owners

Then invoke **req-context**. If gaps remain, ask the next batch — do not close with “add a resume” while the role is still TBD.

Optional: write `resume.md` only if they already named a candidate.
