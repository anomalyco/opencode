Fill the requisition workspace already scaffolded under `.moks/` at `${path}`.

Default `/init` bootstraps a hiring req, not a coding AGENTS.md bible. (Coding AGENTS.md setup is `/init-code` / `build` agent — not this path.)

User-provided focus or constraints (honor these):
$ARGUMENTS

## Goal

A local hiring workspace so recruit can load req materials, score candidates, draft outreach, and record dispositions.

The scaffolder already wrote stubs if they were missing or empty:
- `.moks/req/jd.md`
- `.moks/req/scorecard.md`
- `.moks/req/notes.md`
- `.moks/req/scores/.gitkeep`

Do not create directories — they should already be there. Fill stubs from `$ARGUMENTS`. Do not overwrite non-empty user content.

## Fill stubs

`.moks/req/jd.md` — role title, team, must-haves / nice-to-haves
`.moks/req/scorecard.md` — dimensions and bar
`.moks/req/notes.md` — owners and open questions

Optional: write `resume.md` only if the user already named a candidate.

## After filling

Run or invoke **req-context** to synthesize a structured req brief and list missing context.

## Hiring skills (use via skill tool)

- **req-context** — structured req brief from JD/notes; list gaps
- **score-candidate** — resume vs JD/scorecard with evidence citations; writes `.moks/req/scores/`
- **draft-outreach** — recruiter email/LinkedIn drafts; never send
- **commit-disposition** — recommend reject/offer/hire/advance; user records via decision verbs

## Decision verbs (authority)

Dispositions are receipts only until push. Prefer bash for these CLIs:

- `moks commit` — record disposition intent (local receipt)
- `moks status` — inspect pending / open decisions
- `moks push` — push a committed decision (records push receipt; adverse reject/offer/hire need confirm)

Never invent ATS stage moves or claim a remote write happened.

## Done criteria

- `.moks/req/jd.md`, `.moks/req/scorecard.md`, and `.moks/req/notes.md` exist (they should)
- `.moks/req/scores/` exists
- Short summary of what was filled vs left as stub
- Next step: add a resume → **score-candidate** (writes `.moks/req/scores/<slug>.md`)
