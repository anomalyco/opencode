Add or fill a requisition in the hiring book at `${path}`.

Default `/init` adds a req under `.moks/reqs/<slug>`, not a coding AGENTS.md bible. (Coding AGENTS.md setup is `/init-code` / `build` agent — not this path.)

If `${slug}` is `<slug>`, ask once for a role title, slugify it (lowercase, hyphens), create `.moks/reqs/<that-slug>/`, then fill stubs. If stubs already exist at `${req}`, fill those — do not create directories.

User-provided focus or constraints (honor these):
$ARGUMENTS

## Goal

A local hiring book so recruit can @ a req slug, load materials, score candidates, draft outreach, and record dispositions. One workspace holds many reqs. Do not create a new git repo per req. Do not use a singleton `.moks/req`.

Slug: `${slug}`
Req path: `${req}`
Existing reqs ( @ these slugs ): ${existing}

The scaffolder already wrote stubs if they were missing or empty:
- `${req}/jd.md`
- `${req}/scorecard.md`
- `${req}/notes.md`
- `${req}/scores/.gitkeep`

Fill stubs from `$ARGUMENTS`. Do not overwrite non-empty user content. Do not touch other reqs.

## Fill stubs

`${req}/jd.md` — role title, team, must-haves / nice-to-haves
`${req}/scorecard.md` — dimensions and bar
`${req}/notes.md` — owners and open questions

Optional: write `resume.md` only if the user already named a candidate.

## After filling

Run or invoke **req-context** to synthesize a structured req brief and list missing context.

## Hiring skills (use via skill tool)

- **req-context** — structured req brief from JD/notes; list gaps
- **score-candidate** — resume vs JD/scorecard with evidence citations; writes `${req}/scores/`
- **draft-outreach** — recruiter email/LinkedIn drafts; never send
- **commit-disposition** — recommend reject/offer/hire/advance; user records via decision verbs

## Decision verbs (authority)

Dispositions are receipts only until push. Prefer bash for these CLIs:

- `moks commit` — record disposition intent (local receipt)
- `moks status` — inspect pending / open decisions
- `moks push` — push a committed decision (records push receipt; adverse reject/offer/hire need confirm)

Never invent ATS stage moves or claim a remote write happened.

## Done criteria

- `${req}/jd.md`, `${req}/scorecard.md`, and `${req}/notes.md` exist (they should)
- `${req}/scores/` exists
- Short summary of what was filled vs left as stub
- Next step: @${slug} later, or add a resume → **score-candidate** (writes `${req}/scores/<candidate-slug>.md`)
