// packages/app/src/hooks/useWebSocketSync.ts
// Phase 1 stub — server-side WS binary diff stream ready; client integration in Phase 2
export function useWebSocketSync() {
  return { connected: false, status: "disabled" as const }
}
