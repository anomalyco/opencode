const editID: string | null = null;
const isEditing = !!editID;
console.log("null isEditing:", isEditing)

const editID2: string | null = "foo";
const isEditing2 = !!editID2;
console.log("foo isEditing:", isEditing2)
