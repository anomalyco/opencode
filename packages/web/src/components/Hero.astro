---
// @ts-nocheck
import ThemeHead from "toolbeam-docs-theme/overrides/Head.astro"
import Lander from "./Lander.astro"

const { slug } = Astro.locals.starlightRoute.entry
---

{ slug === ""
  ? <Lander {...Astro.props} />
  : <ThemeHead {...Astro.props}><slot /></ThemeHead>
}
