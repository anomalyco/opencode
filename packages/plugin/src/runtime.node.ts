// The Node executable serves `@opencode/plugin` to loaded plugins through the
// module hooks registered in its bundle prelude (packages/cli/vite.node.config.ts).
export function ensureRuntimeModules() {}
