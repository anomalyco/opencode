import { Skill } from "@opencode-ai/schema/skill"

// Domains supply value schemas here; defaults and behavior remain domain-owned.
export const definitions = {
  "skill.activation": Skill.Activation,
}
