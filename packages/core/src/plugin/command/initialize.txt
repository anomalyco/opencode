Open recruiter intake for a req under `.moks/reqs/<slug>` at `${path}`.

This is hiring intake, not a coding AGENTS.md setup. (`/init-code` / `build` is the coding escape hatch.)

User notes (honor these; they are not a finished JD):
$ARGUMENTS

## Goal

One hiring workspace holds many reqs. Each req is a directory: `.moks/reqs/<slug>/`. Users @ the slug to work in that req. Do not create a git repo per req. Do not use a singleton `.moks/req`.

## Steps

1. Derive `<slug>` from `$ARGUMENTS` (lowercase, hyphens). If missing or the slug looks like a placeholder (numeric like `00001`, not a role title), ask for the role title. Keep an existing folder — do not create a new directory when stubs already exist.

2. Create directories if missing:
   - `.moks/reqs/<slug>/`
   - `.moks/reqs/<slug>/scores/`

3. Scaffold stubs under `.moks/reqs/<slug>/` when missing (do not overwrite non-empty user content):
   - `jd.md` — only confirmed title / team / level / must-haves; leave the rest TBD
   - `scorecard.md` — dimensions and interview bar (1–5 or pass/fail)
   - `notes.md` — process notes, owners, open questions
   - Optional: `resume.md` only if the user already named a candidate

4. Gitignore: if a `.gitignore` exists at `${path}`, ensure a `.moks/` entry is present; add it if missing. Do not create a root `.gitignore` solely for this.

5. This is recruiter intake. After each answer, update the stubs, then use the question tool for the next 2–4 gaps (title → level → team/HM → location → must-haves). Keep asking until the role is staffable. Do not invent a polished JD from a title. Do not stop after the title.

6. After writing what you know, invoke **req-context**. If gaps remain, ask the next batch — do not close with “add a resume” while the role is still TBD.
