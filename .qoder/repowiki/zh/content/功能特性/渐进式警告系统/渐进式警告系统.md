# 渐进式警告系统

<cite>
**Referenced Files in This Document**   
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js)
- [ProgressiveWarningSystem.js](file://context-claude-code/src/claude-core/ProgressiveWarningSystem.js)
- [index.js](file://context-claude-code/src/types/index.js)
</cite>

## 目录
1. [简介](#简介)
2. [三级警告体系](#三级警告体系)
3. [内存状态评估](#内存状态评估)
4. [进度条生成](#进度条生成)
5. [警告趋势分析](#警告趋势分析)
6. [配置与使用](#配置与使用)

## 简介

渐进式警告系统（Progressive Warning System）是一个智能内存管理机制，旨在通过三级预警体系监控和管理系统的记忆使用情况。该系统基于内存使用率、冷却时间和警告历史等多维度因素，提供从温和提醒到自动压缩的渐进式响应策略。系统通过`assessMemoryStatus`函数评估当前内存状态，利用`generateProgressBar`函数生成直观的可视化进度条，并通过`getTrendAnalysis`函数分析警告趋势，为用户提供全面的内存使用洞察。

**Section sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L11-L480)
- [ProgressiveWarningSystem.js](file://context-claude-code/src/claude-core/ProgressiveWarningSystem.js#L11-L505)

## 三级警告体系

渐进式警告系统采用三级预警机制，根据内存使用率的不同阈值触发相应的警告级别和操作建议。该体系由三个核心函数构成：`_W5`函数（60%警告阈值）、`jW5`函数（80%紧急阈值）和`h11`函数（92%临界阈值）。

```mermaid
graph TD
A[正常状态] --> |使用率 ≥ 60%| B[警告状态]
B --> |使用率 ≥ 80%| C[紧急状态]
C --> |使用率 ≥ 92%| D[临界状态]
subgraph "警告级别"
A --> |0-59%| Normal["正常 (NORMAL)"]
B --> |60-79%| Warning["警告 (WARNING)"]
C --> |80-91%| Urgent["紧急 (URGENT)"]
D --> |≥92%| Critical["临界 (CRITICAL)"]
end
subgraph "用户响应"
Warning --> |温和提醒| Gentle["温和提醒"]
Urgent --> |建议手动整理| Manual["建议手动整理"]
Critical --> |自动压缩| Auto["触发自动压缩"]
end
```

**Diagram sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L11-L480)

**Section sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L11-L480)

## 内存状态评估

`assessMemoryStatus`函数是渐进式警告系统的核心，负责评估当前内存状态并生成相应的警告级别和用户消息。该函数结合内存使用率、冷却时间和警告历史等多维度因素，提供精准的内存状态评估。

### 评估流程

```mermaid
flowchart TD
Start([开始评估]) --> Calculate["计算使用率百分比"]
Calculate --> CheckCooldown["检查冷却时间"]
CheckCooldown --> |未过冷却期| ReturnLast["返回最后消息"]
CheckCooldown --> |已过冷却期| CheckCritical["检查临界阈值(92%)"]
CheckCritical --> |≥92%| Critical["临界状态: 触发自动压缩"]
CheckCritical --> |<92%| CheckUrgent["检查紧急阈值(80%)"]
CheckUrgent --> |≥80%| Urgent["紧急状态: 建议手动整理"]
CheckUrgent --> |<80%| CheckWarning["检查警告阈值(60%)"]
CheckWarning --> |≥60%| Warning["警告状态: 温和提醒"]
CheckWarning --> |<60%| Normal["正常状态: 记忆健康"]
Critical --> UpdateStatus["更新状态和历史"]
Urgent --> UpdateStatus
Warning --> UpdateStatus
Normal --> UpdateStatus
UpdateStatus --> GenerateBar["生成进度条"]
GenerateBar --> ReturnResult["返回完整状态对象"]
```

### 警告级别定义

| 警告级别 | 阈值 | 触发函数 | 用户消息 | 建议操作 |
|---------|------|---------|---------|---------|
| **正常** | 0-59% | - | ✅ 记忆健康 | 继续对话 |
| **警告** | 60-79% | _W5 | 💡 记忆使用量较高 | 温和提醒 |
| **紧急** | 80-91% | jW5 | ⚠️ 记忆空间紧张，建议手动整理 | 建议手动整理 |
| **临界** | ≥92% | h11 | 🚨 记忆空间已满，正在整理... | 触发自动压缩 |

**Section sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L55-L147)

## 进度条生成

`generateProgressBar`函数负责生成直观的可视化进度条，使用不同颜色和填充符号展示内存压力。进度条通过颜色、填充符号和百分比数字的组合，为用户提供清晰的内存使用情况可视化。

### 进度条样式

```mermaid
classDiagram
class ProgressBarGenerator {
+generateProgressBar(percentage : number) : string
-getFillColor(percentage : number) : string
-getFillSymbol(percentage : number) : string
-getEmptySymbol() : string
}
class ColorScheme {
+RED : "🔴"
+YELLOW : "🟡"
+GREEN : "🟢"
+BLUE : "🔵"
}
class FillSymbol {
+SOLID : "█"
+DARK : "▓"
+MEDIUM : "▒"
+LIGHT : "░"
}
ProgressBarGenerator --> ColorScheme : "使用"
ProgressBarGenerator --> FillSymbol : "使用"
```

### 进度条生成逻辑

```mermaid
flowchart TD
Start([开始生成]) --> CheckEnabled["检查是否启用进度条"]
CheckEnabled --> |禁用| ReturnEmpty["返回空字符串"]
CheckEnabled --> |启用| CalculateLength["计算填充长度"]
CalculateLength --> DetermineStyle["确定样式"]
DetermineStyle --> |≥92%| CriticalStyle["红色实心 █"]
DetermineStyle --> |80-91%| UrgentStyle["黄色暗格 ▓"]
DetermineStyle --> |60-79%| WarningStyle["绿色中格 ▒"]
DetermineStyle --> |<60%| NormalStyle["绿色空格 ░"]
CriticalStyle --> FormatOutput["格式化输出"]
UrgentStyle --> FormatOutput
WarningStyle --> FormatOutput
NormalStyle --> FormatOutput
FormatOutput --> ReturnResult["返回进度条字符串"]
```

**Diagram sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L198-L226)

**Section sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L198-L226)

## 警告趋势分析

`getTrendAnalysis`函数基于历史记录分析警告频率的趋势，判断警告频率是增加、减少还是稳定。该函数通过分析最近的警告历史，为用户提供长期的内存使用模式洞察。

### 趋势分析算法

```mermaid
sequenceDiagram
participant System as "系统"
participant Analyzer as "趋势分析器"
participant History as "警告历史"
System->>Analyzer : getTrendAnalysis()
Analyzer->>History : 获取最近10次警告
History-->>Analyzer : 返回警告记录
Analyzer->>Analyzer : 计算警告频率
Analyzer->>Analyzer : 计算平均警告级别
Analyzer->>Analyzer : 判断趋势类型
alt 警告频率 > 70%
Analyzer->>Analyzer : 趋势 = "increasing"
Analyzer->>Analyzer : 生成优化建议
else 警告频率 < 30%
Analyzer->>Analyzer : 趋势 = "decreasing"
Analyzer->>Analyzer : 生成良好反馈
else
Analyzer->>Analyzer : 趋势 = "stable"
Analyzer->>Analyzer : 生成监控建议
end
Analyzer-->>System : 返回趋势分析结果
```

### 趋势判断标准

| 趋势类型 | 频率范围 | 平均级别 | 用户建议 |
|---------|---------|---------|---------|
| **增加** | >70% | 高 | 优化使用模式，增加压缩频率，检查内存泄漏 |
| **稳定** | 30%-70% | 中 | 继续监控使用情况，保持当前模式 |
| **减少** | <30% | 低 | 内存使用模式良好，继续保持 |

**Diagram sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L365-L420)

**Section sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L365-L420)

## 配置与使用

渐进式警告系统提供了灵活的配置选项和使用方法，允许用户根据具体需求调整阈值、获取当前状态以及处理系统发出的警告事件。

### 配置选项

```mermaid
erDiagram
CONFIG ||--o{ THRESHOLDS : "包含"
CONFIG ||--o{ WARNING : "包含"
CONFIG {
json config JSON
number warningCooldown
boolean enableProgressBars
boolean enableEstimates
}
THRESHOLDS {
number normal
number warning
number urgent
number critical
}
WARNING {
string level
string message
string action
number percentage
number estimatedRounds
boolean shouldDisplay
}
```

### 使用示例

```mermaid
sequenceDiagram
participant User as "用户"
participant System as "渐进式警告系统"
participant Memory as "内存管理器"
User->>System : 初始化系统
System->>System : 设置阈值配置
loop 定期检查
Memory->>System : 提供当前使用量和最大token数
System->>System : assessMemoryStatus(currentUsage, maxTokens)
System->>System : 检查冷却时间
alt 超过临界阈值(92%)
System->>System : 触发自动压缩
System-->>User : 显示临界警告
else 超过紧急阈值(80%)
System->>System : 建议手动整理
System-->>User : 显示紧急警告
else 超过警告阈值(60%)
System->>System : 发出温和提醒
System-->>User : 显示警告消息
else
System-->>User : 显示健康状态
end
System->>System : 生成进度条
System->>System : 记录警告历史
end
User->>System : getTrendAnalysis()
System-->>User : 返回趋势分析结果
```

**Section sources**
- [ProgressiveWarningSystem.js](file://context-claude-code/src/warning/ProgressiveWarningSystem.js#L11-L480)
- [index.js](file://context-claude-code/src/types/index.js#L29-L34)