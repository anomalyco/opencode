# Project Audit and Strategy Log

## 1. 監査と環境整備結果 (Audit Results)
- **環境設定の修正**: `.zshrc` の PATH 設定が破損していたため修正し、`bun` コマンドが正常に動作するようにしました。
- **セキュリティ脆弱性**: `bun audit` により 110 件の脆弱性（Critical 2件、High 30件）が発見されました。依存関係の更新を試みましたが、主要な依存パッケージ（`fast-xml-parser` など）のバージョンアップが必要です。
- **MQL5 リファクタリング**:
    - `Expert_Main.mq5` からシグナル実行ロジックを分離し、リスク管理クラス (`CRiskManager`) に一元化しました。
    - ハードコードされていたパラメータ（SL/TP、ファイルパスなど）をクラスのメンバ変数化し、外部から設定可能にしました。
    - `CMLModel_Universal` のインジケーター計算プレースホルダーを、実際の MQL5 関数で実装しました。

## 2. 開発アプローチの決定 (Strategic Decisions)
- **❌ Oracle 統合計画の却下**:
    - **理由**: 設計（Oracle）と実装（Coder）の分離はウォーターフォール開発であり、アルゴリズム取引に必要な「仮説検証サイクル」を破壊します。ドキュメントの肥大化と管理コスト増大を招くため不採用。
- **❌ AI 完全自律ループの修正**:
    - **理由**: AI が自律的にバックテストを行い、合格したら自動でデプロイする案は、過学習やハルシネーションのリスクが高すぎるため却下。
    - **✅ 採用案**: **「AI が仮説立案・コード生成・解析を行い、人間が最終判断（GO/NO-GO）を行う」**というハイブリッド型。AI を「Red Teamer（徹底的なテスター）」として活用します。

## 3. 技術的検討結果 (Technical Analysis)
- **❌ 決定木ハードコード自動生成の却下**:
    - **理由**: 巨大な `if-else` 文の生成は、CPU の分岐予測失敗によるストールで **ONNX より遅くなる**リスクがあります。また、更新のたびに再コンパイルが必要で機動力を失い、数万行のコードはデバッグ不能なスパゲッティ化するため不採用。
    - **✅ 採用技術**: 柔軟性が高く、ファイル差し替えだけで戦略更新が可能な **ONNX 推論**、または AI が発見したパターンの「特徴量重要度」を解釈可能なロジックとして抽出するアプローチ。

## 4. 今後のロードマップ (Future Roadmap)
1.  **前半（基盤強化）**: セキュリティ脆弱性の解消、テスト環境の整備、コード品質の向上。
2.  **後半（EA開発）**:
    - MQL5 設定の外部化完了。
    - 新戦略モジュールの設計と実装。
    - バックテストとデータ収集のパイプライン確立。
