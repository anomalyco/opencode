# 迭代计划: <主题>

> 迭代: <date>-<slug>
> 制定日期: `YYYY-MM-DD`
> 状态: 🔄 执行中
> 评审: P2 LLM Panel (≥5/7)

## 一、迭代目标

<简要描述本次迭代要完成什么，不定版本号>

## 二、去重说明

### 2.1 候选 Issue

| # | Issue | GitHub | 级别 | 描述 |
|:-:|-------|:------:|:----:|------|
| 1 | | | | |

### 2.2 跨 Issue 影响交叉比对

| 对比 | 结论 | 理由 |
| ---- | ---- | ---- |

**判定：<数量> 个语义重复，<处置>**

### 2.3 排除项核查

| 检查项 | 结果 |
| ------ | ---- |
| 过往发布已包含 (CHANGELOG) | |
| 已有关联 PR 已 merge | |
| 已有 open Issue 重复 | |

## 三、冲突检测与排序

### 3.1 同文件/同包冲突矩阵

| 冲突对 | 类型 | 重叠范围 | 处置 |
| ------ | ---- | -------- | ---- |

### 3.2 跨包依赖链

```
<依赖拓扑图>
```

### 3.3 构建依赖链

> 从 `turbo.json` 和跨包引用扫描提取，确保 P6 执行顺序不破坏构建。

| 依赖方向 | 来源 | 约束 |
|---------|------|------|
| A#build → B#build | turbo.json `dependsOn` | A P6 完成前 B 不能开始 |
| | | |

### 3.4 交叉地带归属检查

> 防止「代码↔文档」等边界文件被所有 Issue 遗漏。

1. 列出所有 Issue 覆盖的文件集
2. 识别与变更主题相关但未被任何 Issue 覆盖的"无人区"文件
3. 明确归入最近的 Issue 或创建独立子 Issue

| 无人区文件 | 关联 Issue | 处置 |
|-----------|:---------:|------|
| | | |

## 四、执行序列

| Wave | Issue | 级别 | 负责 Agent | 前置 |
|:----:|:-----:|:---:|-----------|------|
| 1 | | | | — |

## 五、并发控制

- 单 Agent WIP: 1
- 同包并行: 禁止
- 同文件并行: 强制串行

## 六、检查清单

- [ ] 去重已完成并记录
- [ ] 候选 Issue 均已通过 P1 分级
- [ ] 同包冲突已检测并排序
- [ ] 跨包 API 依赖已识别
- [ ] 构建依赖链已建模（turbo.json + 跨包引用扫描）
- [ ] 交叉地带归属已检查（代码↔文档无人区已覆盖）
- [ ] Fast-track 候选已标识
- [ ] 迭代范围与 Agent 容量匹配（≤3 M+/Agent）
