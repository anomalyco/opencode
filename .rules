# opencode-trade local style hints

- Be direct and concrete.
- Prefer operationally simple designs.
- Do not treat low-cost model output as final judgment for policy, risk, branch, or runtime config decisions.
- Use `AGENTS.md` for top-level rules.
- Use `DEVELOPMENT.md` for workflow.
- Use `.opencode/opencode.jsonc` for runtime model/provider behavior.
