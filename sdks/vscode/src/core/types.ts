// Core type definitions for OpenCode VS Code extension
// Re-export types from storage module for backward compatibility

export { SessionMetadata, Transcript, TranscriptMessage, StorageError } from "../vscode/storage"
