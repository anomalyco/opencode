---

date: 2026-03-01T12:00:00 MST
decisionId: DECISION_120
status: ORACLE_REVIEW_PASS_2_v2
threadConfidence: 78/100
assessment:
internal: CLEAR (88/100)
upstream: CONDITIONAL → CLEAR pending Phase 1 gates
penned: APPEND_ONLY
branch: chasing-jules
commit: 28a5c919a
-----------------

# PROVENANCE REVIEW PASS #2 — v2 (Google Jules Integration)

## 0) What changed vs v1

This v2 keeps the full thread intact, but **tightens boundary language**:

* **MongoDB is not an OpenCode dependency.** MongoDB appears only as **internal Pantheon/local context** in the thread (and as a “constraint story” that motivates the stateless-first design). The upstream lane is still “no persistence.” This aligns with the *Option D stateless proxy* recommendation, which explicitly uses “no persistence needed” as the Phase 1 lever. 

Nothing is deleted. Nothing is rewritten. This is an append-only clarification.

---

## 1) Current state of the thread (grounded)

### Internal lane (Pantheon / your implementation)

* DECISION_120 is recorded as “Implementation In Progress – Milestones 1–3 Complete” with **oracle_approval: 88 (CLEARED – Internal Implementation)**, branch `chasing-jules`, commit `28a5c919a`. 
* The internal plan is explicitly **two-phase** (Phase 1: stateless proxy foundation; Phase 2: SSE-native Agentic Run). 

### Upstream lane (anomalyco/opencode PR posture)

* Upstream is still governed by the “CONDITIONAL → CLEAR” gate list: **issue-first, stateless proxy, new files only, ≥4 test files, human PR text <200 words, strict AGENTS style**, and raw JSON preservation posture. 
* The handoff restates the “instant rejection” red lines (issue-first, no AI PR wall, no modifications to existing provider files, no persistent state, tests required, no `any`). 

---

## 2) Boundary hygiene (the MongoDB confusion, resolved without rewriting history)

### The rule

* **OpenCode upstream scope assumes no MongoDB exists.**
* Any MongoDB references in the thread are treated as **internal Pantheon infrastructure context**, not as an OpenCode architectural requirement.

### Why this is safe

The core upstream recommendation already lands on **Option D: Stateless proxy** precisely because it avoids persistent state (“no persistence needed”). 
So even if the thread contains internal DB talk, the upstream deliverable remains storage-sterile.

---

## 3) Architectural truth (why Jules stays outside ProviderHelper)

The architecture analysis is consistent and now well-supported:

* OpenCode’s `handler.ts` + provider stack assumes **single-request token streaming**, but Jules is a **session lifecycle** (polling, approval gate, terminal artifacts). 
* `provider.ts` requires `ProviderHelper` streaming modifiers (`modifyUrl`, `streamSeparator`, etc.) that do not map to Jules. 
* Conclusion: Jules must be a **standalone domain module** consumed by routes/watchers, not a ProviderHelper adapter. 

---

## 4) Phase plan (what ships when)

### Phase 1 (upstream-friendly)

**Client-driven polling + stateless proxy routes + tests.**

* The architecture journal is explicit: Phase 1 uses **stateless proxy routes** and the **IDE client drives polling**; no watcher, no bus events, no SSE integration yet. 
* This is also the top “Harden Opportunities” guidance: “Scope Phase 1 ruthlessly — proxy routes + types + tests.” 

**Upstream CLEAR gates (must-pass):**

* Create GitHub issue first + maintainer acknowledgement
* Stateless proxy (Option D)
* New files only
* ≥4 test files
* Human-written PR description <200 words
* AGENTS compliance
* Raw JSON preservation posture 

### Phase 2 (SSE-native “Agentic Run”)

**In-memory watcher → publishes `jules.*` bus events → `/event` renders as a truth stream.**

The architecture spec already enumerates the watcher and event taxonomy:

* `POST /jules/watch` starts an in-memory watcher
* watcher publishes `jules.run.*` events (`created/status/plan/activity/artifact/terminal/warning/error`)
* keepalive remains UI-only and explicitly labeled “Status (UI)” 

### Phase 2 dependency (do not ignore)

Your community brief explicitly recommends prioritizing `/event` session filtering (#9650) as a prerequisite for scalable Phase 2 SSE usage. 

---

## 5) UX contract (truth-first and “no theater”)

The research milestone codifies the rules that keep the IDE honest:

* `generateTruthfulKeepalive()` is UI-only, phase-aware, and forbids “success theater.” 
* The SSE/Agentic Run spec hard-rules this split:

  * Timeline = truth stream only
  * Keepalive = UI-only (explicit label)
  * Never claim actions without artifact/activity proof
  * Schema drift must surface as `jules.run.warning` (don’t hide it) 

---

## 6) Oracle verdict (v2)

**Thread confidence remains: 78/100.**
The evidence base is stronger than it was at assessment #1 because:

* the “upstream-acceptable” Phase 1 boundary is explicit and repeatedly reinforced 
* the red lines and CLEAR gates are written down in multiple handoffs 
* Phase 2 is specified but properly gated behind SSE hygiene improvements 

**Primary remaining risk (upstream):** violating red lines (issue-first, new files only, tests, PR text style). 
**Primary remaining risk (Phase 2):** SSE event fanout without filtering; proceed only once #9650 (and ideally bandwidth mitigations) are in a good place. 

---

## 7) Next action set (mechanical)

1. **Phase 1 upstream PR** = proxy routes + tests + strict compliance checklist. 
2. **Phase 2** = watcher + bus events + IDE panel only when #9650 is viable. 

**Signed:** Provenance, Thread-Keeper
