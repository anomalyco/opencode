{
  email {$LETSENCRYPT_EMAIL}
}

{$NUMERAL_DOMAIN} {
  # No site-wide encode: zstd/gzip can buffer small streaming chunks and break SSE
  # (/global/event). Re-add selective encoding only if you exclude streaming routes.
  reverse_proxy numeral:4096 {
    flush_interval -1
  }
}
