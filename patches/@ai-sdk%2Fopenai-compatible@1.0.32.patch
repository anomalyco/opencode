diff --git a/dist/index.mjs b/dist/index.mjs
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -590,7 +590,17 @@ var OpenAICompatibleChatLanguageModel = class {
             }
             if (delta.tool_calls != null) {
               for (const toolCallDelta of delta.tool_calls) {
-                const index = toolCallDelta.index;
+                let index = toolCallDelta.index;
+                if (index == null) {
+                  if (toolCallDelta.id != null) {
+                    const j = toolCalls.findIndex((t) => (t == null ? void 0 : t.id) === toolCallDelta.id);
+                    if (j !== -1) index = j;
+                  }
+                  if (index == null) {
+                    const hole = toolCalls.findIndex((t) => t == null);
+                    index = hole === -1 ? toolCalls.length : hole;
+                  }
+                }
                 if (toolCalls[index] == null) {
                   if (toolCallDelta.id == null) {
                     throw new InvalidResponseDataError({
@@ -616,7 +626,8 @@ var OpenAICompatibleChatLanguageModel = class {
                       name: toolCallDelta.function.name,
                       arguments: (_c = toolCallDelta.function.arguments) != null ? _c : ""
                     },
-                    hasFinished: false
+                    hasFinished: false,
+                    extra_content: toolCallDelta.extra_content
                   };
                   const toolCall2 = toolCalls[index];
                   if (((_d = toolCall2.function) == null ? void 0 : _d.name) != null && ((_e = toolCall2.function) == null ? void 0 : _e.arguments) != null) {
@@ -636,7 +647,14 @@ var OpenAICompatibleChatLanguageModel = class {
                         type: "tool-call",
                         toolCallId: (_f = toolCall2.id) != null ? _f : generateId(),
                         toolName: toolCall2.function.name,
-                        input: toolCall2.function.arguments
+                        input: toolCall2.function.arguments,
+                        ...toolCall2.extra_content != null ? {
+                          providerMetadata: {
+                            openaiCompatible: {
+                              extra_content: toolCall2.extra_content
+                            }
+                          }
+                        } : {}
                       });
                       toolCall2.hasFinished = true;
                     }
@@ -647,6 +665,9 @@ var OpenAICompatibleChatLanguageModel = class {
                 if (toolCall.hasFinished) {
                   continue;
                 }
+                if (toolCallDelta.extra_content != null) {
+                  toolCall.extra_content = toolCallDelta.extra_content;
+                }
                 if (((_g = toolCallDelta.function) == null ? void 0 : _g.arguments) != null) {
                   toolCall.function.arguments += (_i = (_h = toolCallDelta.function) == null ? void 0 : _h.arguments) != null ? _i : "";
                 }
@@ -664,7 +685,14 @@ var OpenAICompatibleChatLanguageModel = class {
                     type: "tool-call",
                     toolCallId: (_m = toolCall.id) != null ? _m : generateId(),
                     toolName: toolCall.function.name,
-                    input: toolCall.function.arguments
+                    input: toolCall.function.arguments,
+                    ...toolCall.extra_content != null ? {
+                      providerMetadata: {
+                        openaiCompatible: {
+                          extra_content: toolCall.extra_content
+                        }
+                      }
+                    } : {}
                   });
                   toolCall.hasFinished = true;
                 }
@@ -690,7 +718,14 @@ var OpenAICompatibleChatLanguageModel = class {
                 type: "tool-call",
                 toolCallId: (_a2 = toolCall.id) != null ? _a2 : generateId(),
                 toolName: toolCall.function.name,
-                input: toolCall.function.arguments
+                input: toolCall.function.arguments,
+                ...toolCall.extra_content != null ? {
+                  providerMetadata: {
+                    openaiCompatible: {
+                      extra_content: toolCall.extra_content
+                    }
+                  }
+                } : {}
               });
             }
             const providerMetadata = {
@@ -778,12 +813,13 @@ var createOpenAICompatibleChatChunkSchema = (errorSchema) => z3.union([
           reasoning: z3.string().nullish(),
           tool_calls: z3.array(
             z3.object({
-              index: z3.number(),
+              index: z3.number().nullish(),
               id: z3.string().nullish(),
               function: z3.object({
                 name: z3.string().nullish(),
                 arguments: z3.string().nullish()
-              })
+              }),
+              extra_content: z3.any().optional()
             })
           ).nullish()
         }).nullish(),
diff --git a/dist/index.js b/dist/index.js
--- a/dist/index.js
+++ b/dist/index.js
@@ -606,7 +606,17 @@ var OpenAICompatibleChatLanguageModel = class {
             }
             if (delta.tool_calls != null) {
               for (const toolCallDelta of delta.tool_calls) {
-                const index = toolCallDelta.index;
+                let index = toolCallDelta.index;
+                if (index == null) {
+                  if (toolCallDelta.id != null) {
+                    const j = toolCalls.findIndex((t) => (t == null ? void 0 : t.id) === toolCallDelta.id);
+                    if (j !== -1) index = j;
+                  }
+                  if (index == null) {
+                    const hole = toolCalls.findIndex((t) => t == null);
+                    index = hole === -1 ? toolCalls.length : hole;
+                  }
+                }
                 if (toolCalls[index] == null) {
                   if (toolCallDelta.id == null) {
                     throw new import_provider3.InvalidResponseDataError({
@@ -632,7 +642,8 @@ var OpenAICompatibleChatLanguageModel = class {
                       name: toolCallDelta.function.name,
                       arguments: (_c = toolCallDelta.function.arguments) != null ? _c : ""
                     },
-                    hasFinished: false
+                    hasFinished: false,
+                    extra_content: toolCallDelta.extra_content
                   };
                   const toolCall2 = toolCalls[index];
                   if (((_d = toolCall2.function) == null ? void 0 : _d.name) != null && ((_e = toolCall2.function) == null ? void 0 : _e.arguments) != null) {
@@ -652,7 +663,14 @@ var OpenAICompatibleChatLanguageModel = class {
                         type: "tool-call",
                         toolCallId: (_f = toolCall2.id) != null ? _f : (0, import_provider_utils2.generateId)(),
                         toolName: toolCall2.function.name,
-                        input: toolCall2.function.arguments
+                        input: toolCall2.function.arguments,
+                        ...toolCall2.extra_content != null ? {
+                          providerMetadata: {
+                            openaiCompatible: {
+                              extra_content: toolCall2.extra_content
+                            }
+                          }
+                        } : {}
                       });
                       toolCall2.hasFinished = true;
                     }
@@ -663,6 +681,9 @@ var OpenAICompatibleChatLanguageModel = class {
                 if (toolCall.hasFinished) {
                   continue;
                 }
+                if (toolCallDelta.extra_content != null) {
+                  toolCall.extra_content = toolCallDelta.extra_content;
+                }
                 if (((_g = toolCallDelta.function) == null ? void 0 : _g.arguments) != null) {
                   toolCall.function.arguments += (_i = (_h = toolCallDelta.function) == null ? void 0 : _h.arguments) != null ? _i : "";
                 }
@@ -680,7 +701,14 @@ var OpenAICompatibleChatLanguageModel = class {
                     type: "tool-call",
                     toolCallId: (_m = toolCall.id) != null ? _m : (0, import_provider_utils2.generateId)(),
                     toolName: toolCall.function.name,
-                    input: toolCall.function.arguments
+                    input: toolCall.function.arguments,
+                    ...toolCall.extra_content != null ? {
+                      providerMetadata: {
+                        openaiCompatible: {
+                          extra_content: toolCall.extra_content
+                        }
+                      }
+                    } : {}
                   });
                   toolCall.hasFinished = true;
                 }
@@ -706,7 +734,14 @@ var OpenAICompatibleChatLanguageModel = class {
                 type: "tool-call",
                 toolCallId: (_a2 = toolCall.id) != null ? _a2 : (0, import_provider_utils2.generateId)(),
                 toolName: toolCall.function.name,
-                input: toolCall.function.arguments
+                input: toolCall.function.arguments,
+                ...toolCall.extra_content != null ? {
+                  providerMetadata: {
+                    openaiCompatible: {
+                      extra_content: toolCall.extra_content
+                    }
+                  }
+                } : {}
               });
             }
             const providerMetadata = {
@@ -794,12 +829,13 @@ var createOpenAICompatibleChatChunkSchema = (errorSchema) => import_v43.z.union(
           reasoning: import_v43.z.string().nullish(),
           tool_calls: import_v43.z.array(
             import_v43.z.object({
-              index: import_v43.z.number(),
+              index: import_v43.z.number().nullish(),
               id: import_v43.z.string().nullish(),
               function: import_v43.z.object({
                 name: import_v43.z.string().nullish(),
                 arguments: import_v43.z.string().nullish()
-              })
+              }),
+              extra_content: import_v43.z.any().optional()
             })
           ).nullish()
         }).nullish(),
