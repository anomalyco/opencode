# 版本计划

> 版本: `vX.Y.Z` | 里程碑: Milestone #N
> 制定日期: `<YYYY-MM-DD>` | 计划发布: `<YYYY-MM-DD>`
> 状态: 草案 / 已评审 / 执行中 / 已发布

## 一、版本目标

<!-- 用 1-2 句话描述本版本的核心目标 -->

## 二、候选 Issue

| #      | Issue     | 级别 | 负责 Agent  | 涉及包        | 依赖 Issue | 排序优先级     |
| ------ | --------- | ---- | ----------- | ------------- | ---------- | -------------- |
| `<id>` | `<title>` | M    | core-dev    | packages/core | —          | 1              |
| `<id>` | `<title>` | S    | feature-dev | packages/ui   | —          | 2              |
| `<id>` | `<title>` | XS   | feature-dev | README.md     | —          | 99(Fast-track) |

## 三、冲突检测与排序

### 3.1 同包冲突矩阵

| 包              | Issue 列表 | 串行/并行         |
| --------------- | ---------- | ----------------- |
| `packages/core` | #100, #101 | 串行（#100→#101） |
| `packages/ui`   | #102       | 单独执行          |

### 3.2 跨包依赖

| 前置 Issue            | 后置 Issue            | 原因     |
| --------------------- | --------------------- | -------- |
| #100（core API 变更） | #102（ui 使用新 API） | 接口依赖 |

### 3.3 执行序列

```
P4 执行顺序:
  批次1: #100(core-dev) ─┐
         #103(feature-dev) ┤ 并行（异包：core ≠ ui）
                           │
  批次2: #101(core-dev)  ─┘ 串行（#100 合入后，同包）

  批次3: #102(feature-dev)  串行（依赖 #100 的新 API）

  Fast-track: #104(随时穿插，不影响主线)
```

## 四、快速轨道

| #      | Issue    | 级别 | 原因（为什么不进版本计划） |
| ------ | -------- | ---- | -------------------------- |
| `<id>` | typo fix | XS   | 无包冲突，随时可做         |

## 五、风险评估

| 风险     | 等级 | 应对     |
| -------- | ---- | -------- |
| `<描述>` | P1   | `<措施>` |

## 六、版本修订记录

| 日期     | 变更          | 原因     |
| -------- | ------------- | -------- |
| `<date>` | 追加 Issue #N | `<原因>` |
| `<date>` | 移除 Issue #N | `<原因>` |

---

## LLM Panel 评审记录

> 评审日期: `<YYYY-MM-DD>`
> 通过模型: N/7
> 评审记录: `.octopus/review/vX.Y-version-plan.md`
