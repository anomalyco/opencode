# MIGRATION

> 版本：`vX.Y.Z`
> 发布日期：`<YYYY-MM-DD>`
> 涉及范围：`<包名或模块>`

## Breaking Change 摘要

<!-- 用 1-2 句话描述本版本引入的破坏性变更 -->

## 迁移步骤

### 1. `<步骤标题>`

**旧行为**：
```bash
# 旧方式
<code>
```

**新行为**：
```bash
# 新方式
<code>
```

### 2. `<步骤标题>`

<!-- 重复上述格式 -->

## 自动迁移

<!-- 如果有自动迁移脚本或逻辑，描述如何使用 -->
```bash
# 启动后自动检测并迁移
# ~/.opencode/ → ~/.octopus/
```

## 兼容窗口

| 旧接口 | 新接口 | 废除计划 |
|--------|--------|---------|
| `<old>` | `<new>` | vX.Y.Z 后 N 个 minor version |

## 常见问题

### Q: `<问题>`
A: `<回答>`

## 反馈

遇到迁移问题？请提交 [Issue](https://github.com/fourbroad/octopus/issues/new)。
