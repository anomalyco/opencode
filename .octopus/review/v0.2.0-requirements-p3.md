# P3 需求分析报告 — v0.2.0 Post-Migration 深度清理

> 日期: 2026-05-12
> 参与 Agent: @core-dev, @feature-dev, @platform, @qa
> 来源: `.octopus/discovery/2026-05-12-post-migration-cleanup.md`

---

## 一、范围修正（基于 domain agent 分析）

### 1.1 Issue 拆解修正

| Issue | 原估算 | 实际发现 | 修正内容 |
|-------|:-----:|:--------:|---------|
| #12 代码&测试清洗 | L (540 文件) | **需拆为 6 个子 Issue**: 文本(200) / 标识符(100) / Effect Tags(50) / HTTP协议(30) / 文件改名(60) / 测试(97) | 单 L 太大，按风险等级拆解 |
| #15 i18n JSON | XS (16 文件) | **实际 51 文件** 在 3 个目录（web/app/desktop） | 严重低估 |
| #16 CI/CD | S (20 文件) | **实际 ~25 文件**（含 4 个 Dockerfile 未纳入原始范围） | 轻微低估 |
| #11 品牌资产 | S (30 文件) | **依赖上游设计稿**，品牌资产分散在 5+ 目录 | 不阻塞其他 Issue |
| #14 翻译文档 | L (578 文件) | **建议决策是否精简语言**（当前 17 种语言维护成本高） | P4 产品决策 |

### 1.2 保留清单补充（§2.10 运行时契约）

| 标识符 | 数量 | 决策 |
|--------|:---:|------|
| `@opencode/...` Effect Context tags | ~93 | 变，需整批原子替换 |
| `x-opencode-*` HTTP headers | ~40 | 变，需 client/server 同步 |
| `opencode://` deep link 协议 | ~20 | 变，需 OS 注册同步 |
| `"opencode"` provider ID string | ~32 | **保留内部值**，仅改 UI 标签 |
| `opencode.db` 数据库文件名 | ~4 | 提供迁移路径而非直接改名 |
| `opencode-process.ts` 模块名 | ~5 | 变：git mv |
| `virtual:opencode-server` | ~3 | 变 |
| `schema.make("opencode")` | ~2 | 保留 |

---

## 二、Issue #12 建议拆解方案（core-dev 输出）

```
┌─ 12a: 纯文本/注释/日志清洗 (~200 文件, 🟢 低风险)
├─ 12b: 标识符重命名 (~100 文件, 🟡 中风险)
├─ 12c: Effect Tags 原子替换 (~50 文件, 🔴 高风险) ← 需 12a 前置
├─ 12d: HTTP Headers / Deep Link (~30 文件, 🔴 极高风险) ← 需 12c 前置
├─ 12e: 文件/模块改名 (~60 文件, 🟡 中风险) ← 可并行 12a
└─ 12f: 测试文件清洗 (~97 文件, 🟡 中风险) ← 需 12a~e 全部完成后
```

**总工时估算**: 18-26 agent-hours (~5 人天)

**验收关键**:
- DI 标签 93 处→93 处原子替换无遗漏
- HTTP header 名在 server/sdk/app 三端一致
- `opencode.db` 提供迁移路径（旧文件→提示迁移）
- `bun turbo typecheck` + 全量 test:ci 通过

---

## 三、质量保障策略（qa 输出）

### 3.1 质量门层级

| 层级 | 内容 | 执行时机 |
|------|------|---------|
| G0 (标准门) | oxlint, prettier, typecheck, frozen-lockfile, test:ci, HttpApi, Playwright, audit | CI 自动 |
| G1 (替换正确性门) | 残留零容忍、保留清单验证、scope/URL/registry 检查 | P7 前 |
| G2 (语义一致性门) | CLI 触发词、bot 用户名、class 替换抽查 | P7 人工/LLM |
| G3 (构建产物门) | SDK 导出名、CLI binary、turbo 任务名、Docker tag | P7 |

### 3.2 需要新增的测试

1. `packages/octopus/test/rebrand/residual.test.ts` — 残留检测集成到 `bun test`
2. `packages/sdk/js/src/__tests__/export-names.test.ts` — SDK 导出名验证
3. CLI smoke 增强 — `octopus --help` 不含 "opencode"
4. 环境变量双轨测试 — `OCTOPUS_*` 优先，`OPENCODE_*` fallback

### 3.3 需要新增的验证脚本

`script/verify-rebrand-v2.ts` — 基于规则文件驱动，支持 `--ci`/`--human`/`--json` 模式
读取 `.octopus/config-preview/v0.2.0-replacement-rules.md` 作为单一验证事实来源。

---

## 四、CI/CD & Infra 执行建议（platform 输出）

### 4.1 执行序列（4 阶段）

| Phase | 内容 | 外部依赖 | 风险 |
|-------|------|---------|:---:|
| 1 | safe 纯文本替换（workflow 名称、Dockerfile、VSCode README、specs） | 无 | 🟢 |
| 2 | CI 命令/路径（octopus.yml 触发词、CLI 命令） | 确认 .octopus/agent/translator.md 存在 | 🟡 |
| 3 | Infra 域名（stage.ts DNS、monitoring） | DNS + Cloudflare zone 变更 | 🔴 |
| 4 | 仓库迁移（publish.yml 的 repo check） | fourbroad/octopus 迁移完成 | 🔴 |

### 4.2 待排查未解决问题

1. `ghcr.io/octopus-ai` 注册表是否已创建？
2. `octopus.dev` 域名是否已注册？
3. `opencode-agent[bot]` GitHub App 是否已重命名？
4. `fourbroad/octopus` 仓库迁移是否已完成？
5. PlanetScale 数据库名 `"opencode"` 是否保留？

---

## 五、Feature-dev 执行建议（feature-dev 输出）

### 5.1 执行顺序

```
Wave 1 (立即):
  #10 README 精简  → git rm 21 文件, 3 分钟
  #15 i18n 清洗    → 3 目录 51 文件, 30 分钟

Wave 2 (#13 → #11 → #14):
  #13 英文文档     → 34 篇 MDX, 规则驱动替换
  #11 品牌资产     → 依赖设计稿就绪（上游 vi-design）
  #14 翻译文档     → 578 篇, 脚本批量 + 抽样审查

Wave 3:
  全仓 rg 'opencode' 验证
```

### 5.2 决策建议：翻译文档语言精简

| 方案 | 保留语言 | 文件数 | 理由 |
|------|---------|:-----:|------|
| 3 语言 | en + zh-cn + ja | ~102 | 核心亚洲受众 |
| 5 语言 | en + zh-cn + ja + ko + es | ~170 | 覆盖亚欧主要市场 |
| 17 全留 | 全部 | 578 | 维护成本高，但全球覆盖 |

**建议 P4 评审时人类拍板**。

---

## 六、汇总 Issue 列表（修正后）

| # | Issue 标题 | 级别 | 负责 Agent | 文件数 | 依赖 |
|---|-----------|:---:|-----------|:-----:|:----:|
| 17 | 统一替换规则 & 保留清单 | XS | orchestrator | 1 | — |
| 10 | README 多语言精简 | S | feature-dev | ~22 | #17 |
| 11 | 品牌资产替换 | S | feature-dev + vi-design | ~30 | #17 |
| 12a | 纯文本/日志/注释清洗 | M | core-dev | ~200 | #17 |
| 12b | 标识符重命名 | S | core-dev | ~100 | #17 |
| 12c | Effect Tags 原子替换 | S | core-dev | ~50 | #12a |
| 12d | HTTP Headers / Deep Link | S | core-dev | ~30 | #12c |
| 12e | 文件/模块改名 | S | core-dev | ~60 | #17 |
| 12f | 测试文件清洗 | M | core-dev | ~97 | #12a~e |
| 13 | 英文文档清洗 | S | feature-dev | 34 | #17 |
| 14 | 翻译文档清洗 | L | feature-dev | 578 | #13, #17 |
| 15 | i18n 清洗 | S | feature-dev | ~51 | #17 |
| 16 | CI/CD & 脚本 URL | S | platform | ~25 | #17 |

**总计: 14 Issues, ~1,278 文件**

---

## 七、决策待办

| # | 决策 | 由谁拍板 | 影响 |
|---|------|---------|------|
| D1 | 翻译文档精简到 3/5/17 语言？ | 产品/人类 | #14 范围减 85% 或不变 |
| D2 | `"opencode"` provider ID 是否内部保留？ | architect | #12d 协议层范围 |
| D3 | `ghcr.io/octopus-ai` 注册表存在？ | ops/infra | #16 Phase 3 前置条件 |
| D4 | `octopus.dev` 域名状态？ | ops/infra | #16 Phase 3 前置条件 |

---

## 八、P4 评审

本报告已包含来自 @core-dev, @feature-dev, @platform, @qa 的领域分析。
建议进入 P4 LLM Panel 评审。
